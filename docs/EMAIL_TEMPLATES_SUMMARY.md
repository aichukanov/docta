# Email Templates Localization - Summary

## Что сделано

### ✅ Новые файлы

1. **`server/utils/email-templates.ts`** (610 строк)
   - Локализованные шаблоны для всех типов email
   - 4 типа шаблонов × 6 языков = 24 варианта
   - Типобезопасные интерфейсы

2. **`server/utils/user-locale.ts`** (120 строк)
   - Определение локали пользователя
   - Приоритет: Google OAuth → Accept-Language → default (SR)
   - Поддержка всех языков из use-locale

3. **`server/api/test/email-preview.get.ts`** (80 строк)
   - API endpoint для preview email (dev only)
   - Поддержка всех типов и языков

4. **`pages/dev/email-preview.vue`** (180 строк)
   - UI для просмотра email шаблонов
   - Выбор типа и языка
   - Live preview в iframe

5. **`docs/EMAIL_LOCALIZATION.md`** (полная документация)
6. **`docs/EMAIL_TEMPLATES_README.md`** (quick reference)

### 🔄 Обновленные файлы

1. **`server/utils/email.ts`**
   - Добавлен параметр `locale` во все функции
   - Импорт функций из email-templates
   - Сокращено с 309 до 172 строк

2. **`server/utils/email-change.ts`**
   - Возвращает `oldEmail` для уведомлений
   - Обновлен тип возвращаемого значения

3. **`server/api/auth/register.post.ts`**
   - Добавлено определение локали
   - Передача locale в sendEmailVerification

4. **`server/api/auth/resend-verification.post.ts`**
   - Добавлено определение локали
   - Передача locale в sendEmailVerification

5. **`server/api/auth/request-email-change.post.ts`**
   - Добавлено определение локали
   - Передача locale в sendEmailVerification

6. **`server/api/auth/forgot-password.post.ts`**
   - Добавлено определение локали
   - Передача locale в sendPasswordResetEmail

7. **`server/api/auth/confirm-email-change.get.ts`**
   - Реализовано отправка уведомления на старый email
   - Добавлено определение локали

## Поддерживаемые языки

| Язык | Код | Статус |
|------|-----|--------|
| 🇷🇸 Serbian (Latin) | `sr` | ✅ |
| 🇷🇸 Serbian (Cyrillic) | `sr-cyrl` | ✅ |
| 🇬🇧 English | `en` | ✅ |
| 🇷🇺 Russian | `ru` | ✅ |
| 🇩🇪 German | `de` | ✅ |
| 🇹🇷 Turkish | `tr` | ✅ |

## Типы email шаблонов

1. ✅ **Password Reset** - Восстановление пароля
2. ✅ **Email Verification** - Подтверждение email
3. ✅ **Login Notification** - Уведомление о входе
4. ✅ **Email Change Notification** - Уведомление об изменении email

## Как работает определение локали

```
┌─────────────────────────────────────┐
│ getUserLocale(userId, event)        │
└─────────────────────────────────────┘
                 │
                 ▼
    ┌────────────────────────┐
    │ 1. Google OAuth Profile│ ← Если пользователь входил через Google
    └────────────────────────┘
                 │ (null)
                 ▼
    ┌────────────────────────┐
    │ 2. Accept-Language     │ ← Из HTTP header запроса
    └────────────────────────┘
                 │ (null)
                 ▼
    ┌────────────────────────┐
    │ 3. Default (Serbian)   │ ← Локаль по умолчанию
    └────────────────────────┘
```

## Использование

### В API endpoint

```typescript
import { getUserLocale } from '~/server/utils/user-locale';
import { sendPasswordResetEmail } from '~/server/utils/email';

// Определяем локаль
const locale = await getUserLocale(userId, event);

// Отправляем email
await sendPasswordResetEmail(email, resetUrl, locale);
```

### Preview в браузере (dev only)

1. Запустите dev server: `npm run dev`
2. Откройте: http://localhost:3000/dev/email-preview
3. Выберите тип шаблона и язык

## Архитектура

```
┌─────────────────────────────────────────────────────────┐
│                    API Endpoints                        │
│  /api/auth/register, /api/auth/forgot-password, etc.   │
└─────────────────────────────────────────────────────────┘
                         │
                         │ getUserLocale(userId, event)
                         ▼
┌─────────────────────────────────────────────────────────┐
│              server/utils/user-locale.ts                │
│  Определяет локаль: Google → Accept-Language → default  │
└─────────────────────────────────────────────────────────┘
                         │
                         │ locale: Language
                         ▼
┌─────────────────────────────────────────────────────────┐
│               server/utils/email.ts                     │
│  sendPasswordResetEmail(email, url, locale)             │
│  sendEmailVerification(email, url, name, locale)        │
└─────────────────────────────────────────────────────────┘
                         │
                         │ getPasswordResetEmail(locale, url)
                         ▼
┌─────────────────────────────────────────────────────────┐
│           server/utils/email-templates.ts               │
│  Возвращает { subject, html, text } на нужном языке     │
└─────────────────────────────────────────────────────────┘
                         │
                         │ { subject, html, text }
                         ▼
┌─────────────────────────────────────────────────────────┐
│                   Mailgun API                           │
│            Отправка email пользователю                  │
└─────────────────────────────────────────────────────────┘
```

## Что НЕ изменилось

- ✅ Mailgun конфигурация осталась прежней
- ✅ Структура базы данных не изменена
- ✅ API endpoints имеют обратную совместимость (locale опциональный)
- ✅ Development mock остался (логирование вместо отправки)

## Следующие шаги (опционально)

1. Добавить колонку `preferred_locale` в таблицу `auth_users`
2. Позволить пользователям выбирать язык email в профиле
3. Добавить больше языков (FR, IT, ES, etc.)
4. A/B тестирование subject lines
5. Использовать шаблонизаторы (Handlebars, Pug)

## Тестирование

### Manual Testing

```bash
# 1. Запустить dev server
npm run dev

# 2. Открыть preview
open http://localhost:3000/dev/email-preview

# 3. Протестировать все комбинации
# - 4 типа × 6 языков = 24 варианта
```

### API Testing

```bash
# Password Reset (RU)
curl "http://localhost:3000/api/test/email-preview?type=password-reset&locale=ru"

# Email Verification (EN)
curl "http://localhost:3000/api/test/email-preview?type=email-verification&locale=en"

# Login Notification (DE)
curl "http://localhost:3000/api/test/email-preview?type=login-notification&locale=de"

# Email Change (TR)
curl "http://localhost:3000/api/test/email-preview?type=email-change&locale=tr"
```

## Статистика

- **Новых файлов:** 6
- **Обновленных файлов:** 7
- **Добавлено строк кода:** ~1000
- **Удалено строк кода (дубликаты):** ~200
- **Поддерживаемых языков:** 6
- **Email шаблонов:** 4 типа × 6 языков = 24

## Принципы

✅ **DRY** - Все HTML генерируется через `generateEmailHTML()`  
✅ **Type Safety** - TypeScript интерфейсы для всех шаблонов  
✅ **Fallback** - Если локаль не найдена → English  
✅ **Security** - Не раскрываем существование пользователей  
✅ **UX** - Кнопка + ссылка в каждом email  
✅ **Modern Design** - Градиенты, современная типографика  

---

**Готово!** 🎉

Все email шаблоны локализованы на 6 языков с автоматическим определением языка пользователя.
