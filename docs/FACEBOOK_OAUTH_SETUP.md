# Настройка Facebook OAuth для docta.me

## 📋 Содержание

1. [Создание Facebook App](#создание-facebook-app)
2. [Настройка OAuth](#настройка-oauth)
3. [Получение учетных данных](#получение-учетных-данных)
4. [Настройка проекта](#настройка-проекта)
5. [Тестирование](#тестирование)
6. [Публикация приложения](#публикация-приложения)
7. [Troubleshooting](#troubleshooting)

---

## Создание Facebook App

### Шаг 1: Создайте аккаунт разработчика

1. Перейдите на [Facebook for Developers](https://developers.facebook.com/)
2. Войдите в свой Facebook аккаунт
3. Если это первый раз, зарегистрируйтесь как разработчик

### Шаг 2: Создайте новое приложение

1. Нажмите **"Создать приложение"** или **"My Apps"** → **"Create App"**
2. Выберите тип приложения: **"Consumer"** (для входа пользователей)
3. Заполните основную информацию:
   - **App Name**: `docta.me` (или ваше название)
   - **App Contact Email**: ваш email
   - **App Purpose**: выберите подходящий вариант (например, "Yourself or your own business")
4. Нажмите **"Create App"**

---

## Настройка OAuth

### Шаг 3: Добавьте Facebook Login

1. В дашборде приложения найдите **"Add Product"** в левом меню
2. Найдите **"Facebook Login"** и нажмите **"Set Up"**
3. Выберите платформу: **"Web"**
4. Укажите Site URL:
   - Для разработки: `http://localhost:3000`
   - Для продакшена: `https://docta.me`

### Шаг 4: Настройте Valid OAuth Redirect URIs

1. В левом меню перейдите в **"Facebook Login"** → **"Settings"**
2. В поле **"Valid OAuth Redirect URIs"** добавьте:

**Для разработки:**
```
http://localhost:3000/api/auth/callback/facebook
```

**Для продакшена:**
```
https://docta.me/api/auth/callback/facebook
```

3. Сохраните изменения

---

## Получение учетных данных

### Шаг 5: Получите App ID и App Secret

1. В левом меню перейдите в **"Settings"** → **"Basic"**
2. Скопируйте:
   - **App ID** (это будет `FACEBOOK_APP_ID`)
   - **App Secret** (нажмите "Show", чтобы увидеть; это будет `FACEBOOK_APP_SECRET`)

⚠️ **ВАЖНО**: Никогда не публикуйте App Secret в публичных репозиториях!

---

## Настройка проекта

### Шаг 6: Добавьте переменные окружения

Откройте файл `.env` в корне проекта и добавьте:

```env
# Facebook OAuth
FACEBOOK_APP_ID=your_facebook_app_id_here
FACEBOOK_APP_SECRET=your_facebook_app_secret_here
```

**Для продакшена** также убедитесь, что указан правильный `BASE_URL`:

```env
BASE_URL=https://docta.me
```

### Шаг 7: Примените миграцию базы данных

Выполните SQL миграцию для создания таблицы профилей Facebook:

```bash
# Из корня проекта
mysql -u your_user -p your_database < server/sql/migrations/006_facebook_oauth_profiles.sql
```

Или выполните SQL напрямую в вашей БД:

```sql
-- См. содержимое файла server/sql/migrations/006_facebook_oauth_profiles.sql
```

---

## Тестирование

### Шаг 8: Локальное тестирование

1. Запустите сервер разработки:
```bash
npm run dev
```

2. Откройте браузер и перейдите на `http://localhost:3000/login`

3. Нажмите кнопку **"Войти через Facebook"**

4. Вы будете перенаправлены на страницу входа Facebook

5. После авторизации вы вернетесь на сайт

### Проверка данных в БД

После успешной авторизации проверьте таблицы:

```sql
-- Проверка пользователя
SELECT * FROM auth_users WHERE email = 'your_email@example.com';

-- Проверка OAuth аккаунта
SELECT * FROM auth_oauth_accounts WHERE provider = 'facebook';

-- Проверка Facebook профиля
SELECT * FROM auth_oauth_profiles_facebook;
```

---

## Публикация приложения

### Шаг 9: Подготовка к продакшену

⚠️ **ВАЖНО**: По умолчанию Facebook App находится в режиме разработки. Только вы и добавленные тестеры могут использовать авторизацию.

Для публичного использования нужно:

1. В дашборде приложения перейдите в **"App Review"**
2. Выберите **"Permissions and Features"**
3. Запросите разрешения:
   - **email** - доступ к email пользователя (обязательно)
   - **public_profile** - базовая информация о профиле (одобрено автоматически)

4. Заполните необходимую документацию:
   - Скриншоты экрана входа
   - Описание как используются данные
   - Privacy Policy URL
   - Terms of Service URL

5. После одобрения переключите приложение в режим **"Live"**:
   - Settings → Basic → App Mode → переключите на "Live"

### Добавление тестеров (для режима разработки)

Если приложение еще в режиме разработки:

1. Перейдите в **"Roles"** → **"Testers"**
2. Добавьте Facebook аккаунты, которые смогут тестировать вход
3. Отправьте им приглашение

---

## Troubleshooting

### Ошибка: "URL Blocked"

**Проблема**: Facebook блокирует redirect URI

**Решение**:
- Убедитесь, что URL добавлен в "Valid OAuth Redirect URIs"
- URL должен точно совпадать (включая протокол и порт)
- Для localhost должен быть `http://localhost:3000/api/auth/callback/facebook`

### Ошибка: "App Not Set Up"

**Проблема**: Приложение не настроено для веб-платформы

**Решение**:
1. Settings → Basic → Add Platform
2. Выберите "Website"
3. Добавьте Site URL

### Ошибка: "email_not_provided"

**Проблема**: Пользователь не предоставил email

**Решение**:
- Email может отсутствовать если пользователь зарегистрировался через телефон
- Или если email не подтвержден в Facebook
- Попросите пользователя подтвердить email или войти другим способом

### Ошибка: "Invalid OAuth access token"

**Проблема**: Неверный или истекший токен

**Решение**:
- Проверьте правильность FACEBOOK_APP_SECRET
- Убедитесь что используете актуальную версию Graph API (v18.0)
- Токены Facebook истекают через ~60 дней

### Приложение работает локально, но не на продакшене

**Проверьте**:
1. Переменные окружения на сервере (FACEBOOK_APP_ID, FACEBOOK_APP_SECRET)
2. BASE_URL в .env должен быть правильным
3. Redirect URI в Facebook настроен для продакшн домена
4. HTTPS включен (Facebook требует HTTPS для продакшена)
5. Приложение переключено в режим "Live"

---

## Полезные ссылки

- [Facebook for Developers](https://developers.facebook.com/)
- [Facebook Login Documentation](https://developers.facebook.com/docs/facebook-login/)
- [Graph API Reference](https://developers.facebook.com/docs/graph-api/)
- [App Review](https://developers.facebook.com/docs/app-review/)
- [Permissions Reference](https://developers.facebook.com/docs/permissions/reference/)

---

## Безопасность

### Рекомендации

1. **Никогда** не коммитьте App Secret в Git
2. Используйте разные приложения для dev и prod
3. Регулярно обновляйте App Secret
4. Включите App Secret Proof для дополнительной безопасности
5. Мониторьте подозрительную активность в App Dashboard

### Ротация ключей

Если App Secret скомпрометирован:

1. Settings → Basic → App Secret → Reset
2. Обновите переменную FACEBOOK_APP_SECRET на всех серверах
3. Перезапустите приложение

---

## Дополнительные возможности

### Получение дополнительных данных

По умолчанию мы запрашиваем только `email` и `public_profile`. Для получения дополнительных данных:

1. Обновите scope в `server/api/auth/facebook.get.ts`:
```typescript
scope: 'email,public_profile,user_birthday,user_location',
```

2. Обновите поля в запросе userInfo:
```typescript
fields: 'id,name,email,picture.width(200).height(200),birthday,location',
```

3. Запросите разрешения в Facebook App Review

### Long-lived tokens

Facebook токены по умолчанию "short-lived" (~1-2 часа). Для получения "long-lived" токенов (60 дней):

```typescript
const longLivedToken = await $fetch<any>(
  'https://graph.facebook.com/v18.0/oauth/access_token',
  {
    query: {
      grant_type: 'fb_exchange_token',
      client_id: facebook.appId,
      client_secret: facebook.appSecret,
      fb_exchange_token: shortLivedToken,
    },
  }
);
```

---

## Changelog

- **2026-01-31**: Первая версия документации
