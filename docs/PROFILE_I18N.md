# Интернационализация страницы профиля

## Изменения

### 1. Создан файл переводов `i18n/profile.ts`

Содержит все тексты для страницы профиля на **6 языках** (en, ru, sr, de, tr, sr-cyrl):

- Заголовки и общие элементы
- OAuth аккаунты и связанные действия
- Безопасность (пароль, сессии)
- История входов
- Формы редактирования (имя, email, пароль)
- Сообщения об успехе и ошибках

**Структура файла:**

```typescript
export default {
	messages: {
		'en': {
			/* переводы */
		},
		'ru': {
			/* переводы */
		},
		'sr': {
			/* переводы */
		},
		'de': {
			/* переводы */
		},
		'tr': {
			/* переводы */
		},
		'sr-cyrl': {
			/* переводы */
		},
	},
};
```

### 2. Обновлена страница `pages/profile.vue`

В компоненте используется `useI18n` с локальной областью видимости:

```javascript
import profileMessages from '~/i18n/profile';

const { t } = useI18n({
	useScope: 'local',
	messages: profileMessages.messages,
});
```

Все жестко закодированные тексты заменены на вызовы `t('код')`:

**До:**

```javascript
ElMessage.success('Аккаунт успешно отвязан');
ElMessage.error('Не удалось отвязать аккаунт');
```

**После:**

```javascript
ElMessage.success(t('accountUnlinked'));
ElMessage.error(t('errorUnlinkAccount'));
```

**В шаблонах:**

```vue
<!-- До -->
<h3>Привязанные аккаунты</h3>
<p>Управляйте способами входа в систему</p>

<!-- После -->
<h3>{{ t('linkedAccounts') }}</h3>
<p>{{ t('linkedAccountsDescription') }}</p>
```

## Использование в новых компонентах

### Шаг 1: Создайте файл переводов `i18n/[component-name].ts`

**⚠️ ВАЖНОЕ ПРАВИЛО:** Все ключи локализации должны начинаться с **ЗАГЛАВНОЙ буквы**.

```typescript
/**
 * [Component Name] Translations
 */

export default {
	messages: {
		'en': {
			Title: 'Title',
			Description: 'Description',
			// ... другие переводы
		},
		'ru': {
			Title: 'Заголовок',
			Description: 'Описание',
			// ... другие переводы
		},
		'sr': {
			Title: 'Naslov',
			Description: 'Opis',
			// ... другие переводы
		},
		'de': {
			Title: 'Titel',
			Description: 'Beschreibung',
			// ... другие переводы
		},
		'tr': {
			Title: 'Başlık',
			Description: 'Açıklama',
			// ... другие переводы
		},
		'sr-cyrl': {
			Title: 'Наслов',
			Description: 'Опис',
			// ... другие переводы
		},
	},
};
```

**Примеры правильного именования:**

- ✅ `PageTitle`, `BtnSave`, `SuccessMessage`, `ErrorSaving`
- ❌ `pageTitle`, `btnSave`, `successMessage`, `errorSaving`

**ВАЖНО:** Все 6 языков обязательны:

- `en` - English
- `ru` - Русский
- `sr` - Srpski (латиница)
- `de` - Deutsch
- `tr` - Türkçe
- `sr-cyrl` - Српски (ћирилица)

### Шаг 2: Используйте в компоненте

#### Вариант A: Один файл переводов

Когда все переводы компонента находятся в одном файле:

```vue
<script setup lang="ts">
import componentMessages from '~/i18n/[component-name]';

const { t } = useI18n({
	useScope: 'local',
	messages: componentMessages.messages,
});

// В коде
ElMessage.success(t('Title'));
ElMessage.success(t('Description'));
</script>

<template>
	<div>
		<h1>{{ t('Title') }}</h1>
		<p>{{ t('Description') }}</p>
		<el-button>{{ t('ButtonLabel') }}</el-button>
	</div>
</template>
```

#### Вариант B: Комбинирование нескольких файлов переводов

Когда нужно использовать переводы из нескольких файлов (например, общие переводы + специфичные для страницы):

```vue
<script setup lang="ts">
import { combineI18nMessages } from '~/i18n/utils';
import articlesI18n from '~/i18n/articles';
import breadcrumbI18n from '~/i18n/breadcrumb';
import specialtyI18n from '~/i18n/specialty';
import cityI18n from '~/i18n/city';

const { t, locale } = useI18n({
	useScope: 'local',
	messages: combineI18nMessages([
		articlesI18n,
		breadcrumbI18n,
		specialtyI18n,
		cityI18n,
	]),
});
</script>

<template>
	<div>
		<!-- Можно использовать ключи из любого файла -->
		<h1>{{ t('Articles') }}</h1>
		<p>{{ t('BreadcrumbHome') }}</p>
		<span>{{ t('Specialty_cardiology') }}</span>
		<span>{{ t('City_podgorica') }}</span>
	</div>
</template>
```

**Когда использовать комбинирование:**

- ✅ Страница использует переводы из нескольких доменов (города, специальности, статьи)
- ✅ Нужны общие переводы (breadcrumbs, navigation) + специфичные для страницы
- ✅ Компонент работает с данными, которые уже имеют свои файлы переводов

**Когда НЕ использовать комбинирование:**

- ❌ Все тексты специфичны только для одного компонента
- ❌ Можно обойтись одним файлом переводов

## Покрытие

✅ Все UI-тексты профиля переведены на 6 языков
✅ Все сообщения об успехе/ошибках переведены
✅ Все placeholder'ы форм переведены
✅ Все подтверждающие диалоги переведены
✅ Все заголовки модальных окон переведены

## Архитектура

Решение следует стандартной архитектуре проекта:

### Простой случай (один файл переводов):

```
pages/profile.vue
  ↓ импортирует
i18n/profile.ts (переводы с 6 языками)
  ↓ передаются в
useI18n({ useScope: 'local', messages: profileMessages.messages })
  ↓ предоставляет
t('код') - функцию для получения переводов
```

### Сложный случай (комбинирование файлов):

```
pages/articles/russian-speaking-doctors-in-montenegro.vue
  ↓ импортирует несколько файлов
i18n/articles.ts
i18n/breadcrumb.ts
i18n/specialty.ts
i18n/city.ts
  ↓ объединяются через
combineI18nMessages([...])
  ↓ передаются в
useI18n({ useScope: 'local', messages: combinedMessages })
  ↓ предоставляет
t('любой_код_из_любого_файла')
```

**Важные моменты:**

- ✅ Не используются composable'ы (избегаем проблем с реактивностью)
- ✅ Использование `messages.messages` для одного файла
- ✅ Использование `combineI18nMessages([...])` для нескольких файлов
- ✅ Структура `export default { messages: { ... } }` как в других файлах проекта
- ✅ Все 6 языков из `use-locale.ts` включены
- ✅ Функция `combineI18nMessages` из `~/i18n/utils` для объединения переводов

## Следующие шаги

Рекомендуется применить такой же подход к другим страницам:

- `pages/login.vue` → `i18n/login.ts`
- `pages/forgot-password.vue` → `i18n/forgot-password.ts`
- `pages/reset-password.vue` → `i18n/reset-password.ts`
- `pages/verify-email.vue` → `i18n/verify-email.ts`
- `pages/confirm-email-change.vue` → `i18n/confirm-email-change.ts`

Для каждой страницы:

1. Создать файл переводов со структурой `export default { messages: { en, ru, sr, de, tr, 'sr-cyrl' } }`
2. Перевести все тексты на 6 языков
3. Импортировать в компонент: `import messages from '~/i18n/[name]'`
4. Инициализировать: `const { t } = useI18n({ useScope: 'local', messages: messages.messages })`
5. Заменить все тексты на вызовы `t('код')`

## 📚 Дополнительная документация

- **[I18N_TEMPLATE.md](./I18N_TEMPLATE.md)** - Шаблоны и подробные примеры создания файлов переводов
- **[I18N_CHEATSHEET.md](./I18N_CHEATSHEET.md)** - Быстрая шпаргалка по i18n (Quick Reference)
