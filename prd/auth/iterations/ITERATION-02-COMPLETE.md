# Итерация 2: Email/Password авторизация для админов - ЗАВЕРШЕНА ✅

**Дата:** 2026-01-30  
**Статус:** 🟢 Completed

## Что реализовано

### 1. Утилиты

📄 **`server/utils/password.ts`**
- ✅ `hashPassword()` - хеширование паролей (bcrypt, cost=10)
- ✅ `comparePassword()` - проверка пароля

📄 **`server/utils/session.ts`**
- ✅ `createSession()` - создание сессии
- ✅ `setSessionCookie()` - установка HTTPOnly cookie
- ✅ `deleteSession()` - удаление сессии
- ✅ `clearSessionCookie()` - очистка cookie
- ✅ `getUserFromSession()` - получение пользователя по сессии
- ✅ `getUserByEmail()` - получение пользователя по email

### 2. Обновленная авторизация

📄 **`server/common/auth.ts`** (ЗАМЕНЕНА ЗАГЛУШКА)
- ✅ `requireAdmin()` - теперь асинхронная, проверяет сессию из БД
- ✅ `getCurrentUser()` - получение текущего пользователя (опционально)
- ✅ Проверка `is_admin` из БД
- ✅ Возврат 401/403 при ошибках авторизации

### 3. API Endpoints

📄 **`server/api/admin/auth/login.post.ts`**
- ✅ POST `/api/admin/auth/login`
- ✅ Проверка email + password
- ✅ Проверка `is_admin=true`
- ✅ Сравнение пароля с bcrypt hash
- ✅ Создание сессии в БД
- ✅ Установка HTTPOnly cookie

📄 **`server/api/admin/auth/logout.post.ts`**
- ✅ POST `/api/admin/auth/logout`
- ✅ Удаление сессии из БД
- ✅ Очистка cookie

📄 **`server/api/admin/auth/me.get.ts`**
- ✅ GET `/api/admin/auth/me`
- ✅ Возврат текущего пользователя

### 4. Frontend

📄 **`pages/admin/login.vue`**
- ✅ Страница входа `/admin/login`
- ✅ Форма с email/password
- ✅ Валидация полей
- ✅ Обработка ошибок
- ✅ Красивый UI (Element Plus)
- ✅ Редирект на `/admin` после успешного входа

📄 **`pages/admin/index.vue`** (обновлена)
- ✅ Добавлен header с email и кнопкой выхода
- ✅ Middleware `admin-auth`
- ✅ Функция logout

📄 **`middleware/admin-auth.ts`**
- ✅ Проверка авторизации через API
- ✅ Редирект на `/admin/login` если не авторизован

### 5. Обновление существующих API

✅ **Обновлены все 24 API endpoint** - добавлен `await requireAdmin(event)`:
- `doctors/*` (6 файлов)
- `labtests/*` (6 файлов)
- `services/*` (6 файлов)
- `clinics/*` (4 файла)
- `billing/*` (2 файла)

## Как использовать

### 1. Применить миграцию (если еще не применена)

```bash
mysql -u your_user -p your_database < server/sql/migrations/001_auth_basic.sql
```

### 2. Создать первого админа

```bash
# Сгенерировать хеш
node -e "const bcrypt = require('bcrypt'); bcrypt.hash('your_password', 10).then(console.log)"

# Вставить в БД
mysql -u your_user -p your_database
INSERT INTO users (email, name, password_hash, is_admin) VALUES
('admin@docta.me', 'Admin', 'HASH_HERE', TRUE);
```

### 3. Запустить приложение

```bash
npm run dev
```

### 4. Войти в админку

1. Открыть `http://localhost:3000/admin/login`
2. Ввести email и пароль
3. Попасть в админку!

## Критерии приемки

- [x] AC-1: Страница `/admin/login` существует и доступна
- [x] AC-2: Форма логина содержит поля email и password
- [x] AC-3: Форма валидирует обязательные поля
- [x] AC-4: При вводе корректных данных администратора происходит успешный вход
- [x] AC-5: При вводе неверного пароля показывается ошибка
- [x] AC-6: При вводе email не-администратора показывается ошибка
- [x] AC-7: После успешного входа создается сессия в БД
- [x] AC-8: После успешного входа устанавливается HTTPOnly cookie
- [x] AC-9: После успешного входа происходит редирект на админскую страницу
- [x] AC-10: Middleware `admin-auth` блокирует доступ к `/admin` для неавторизованных
- [x] AC-11: Middleware пропускает запросы с валидной сессией админа
- [x] AC-12: Endpoint `/api/admin/auth/logout` удаляет сессию из БД
- [x] AC-13: После logout очищается cookie
- [x] AC-14: Пароли хешируются с использованием bcrypt (cost=10)
- [x] AC-15: Все существующие API endpoints обновлены (`await requireAdmin`)

## Тестирование

### Тест 1: Успешный вход

```bash
# 1. Создать админа
node -e "const bcrypt = require('bcrypt'); bcrypt.hash('admin123', 10).then(console.log)"
# Вставить в БД с хешом

# 2. Открыть http://localhost:3000/admin/login
# 3. Ввести: admin@docta.me / admin123
# 4. Ожидается: редирект на /admin
```

### Тест 2: Неверный пароль

```bash
# 1. Открыть /admin/login
# 2. Ввести: admin@docta.me / wrongpassword
# 3. Ожидается: ошибка "Неверный email или пароль"
```

### Тест 3: Не-администратор

```bash
# 1. Создать обычного пользователя (is_admin=false)
# 2. Попробовать войти
# 3. Ожидается: ошибка "Неверный email или пароль"
```

### Тест 4: Middleware

```bash
# 1. Без входа открыть /admin
# 2. Ожидается: редирект на /admin/login
# 3. После входа снова открыть /admin
# 4. Ожидается: доступ разрешен
```

### Тест 5: API защита

```bash
# 1. Без входа: curl http://localhost:3000/api/doctors/admin-details
# 2. Ожидается: 401 Unauthorized
# 3. После входа: повторить запрос с cookie
# 4. Ожидается: данные возвращаются
```

### Тест 6: Logout

```bash
# 1. Войти как админ
# 2. Нажать "Выйти"
# 3. Проверить БД: сессия удалена
# 4. Попробовать открыть /admin
# 5. Ожидается: редирект на /admin/login
```

## Созданные файлы

```
server/
├── utils/
│   ├── password.ts ................................. ✅ Утилиты для паролей
│   └── session.ts .................................. ✅ Утилиты для сессий
├── common/
│   └── auth.ts ..................................... ✅ Обновлен (настоящая авторизация)
└── api/
    └── admin/
        └── auth/
            ├── login.post.ts ....................... ✅ API входа
            ├── logout.post.ts ...................... ✅ API выхода
            └── me.get.ts ........................... ✅ API текущего пользователя

pages/
├── admin/
│   ├── login.vue ................................... ✅ Страница входа
│   └── index.vue ................................... ✅ Обновлена (header + logout)

middleware/
└── admin-auth.ts ................................... ✅ Middleware защиты

Обновлены 24 API endpoint:
├── server/api/doctors/* (6 файлов) ................ ✅
├── server/api/labtests/* (6 файлов) ............... ✅
├── server/api/services/* (6 файлов) ............... ✅
├── server/api/clinics/* (4 файла) ................. ✅
└── server/api/billing/* (2 файла) ................. ✅
```

## Безопасность

✅ **HTTPOnly cookies** - защита от XSS  
✅ **Bcrypt hashing** - надежное хранение паролей (cost=10)  
✅ **Database sessions** - сессии в БД, не в памяти  
✅ **CSRF protection** - sameSite='lax'  
✅ **Session expiration** - автоматическое истечение через 30 дней  
✅ **Admin-only access** - проверка `is_admin=true`  
✅ **No open registration** - админы создаются вручную в БД

## Следующие шаги

✅ **Итерация 1 завершена** - БД готова  
✅ **Итерация 2 завершена** - Авторизация работает

⏭️ **Следующая итерация (опционально):** [Итерация 3 - OAuth через Google](../iterations/iteration-03-oauth-google.md)

Что будет реализовано:
- OAuth через Google для обычных пользователей
- Регистрация через Google
- Привязка нескольких OAuth провайдеров

---

**Прогресс:** 40% (2/5 итераций) ████░░░░░░

**Детали:** См. `prd/auth/PROGRESS.md`

## Важные замечания

⚠️ **Обязательно измените тестовый пароль!**
⚠️ **Используйте сильные пароли (минимум 12 символов)**
⚠️ **Не коммитьте пароли в git**
⚠️ **В production используйте HTTPS (secure cookies)**

🎉 **Авторизация для админки готова!**
