# Итерация 3: OAuth через Google - ЗАВЕРШЕНА ✅

**Дата:** 2026-01-30  
**Статус:** 🟢 Completed

## Что реализовано

### 1. OAuth Infrastructure (без сторонних библиотек!)

📄 **`server/utils/oauth-config.ts`**
- ✅ Конфигурация для Google OAuth
- ✅ URLs для auth, token, userInfo
- ✅ Настройка redirect URI

📄 **`server/utils/oauth.ts`**
- ✅ `findUserByOAuth()` - поиск пользователя по OAuth
- ✅ `createOAuthUser()` - создание нового OAuth пользователя
- ✅ `updateOAuthTokens()` - обновление токенов
- ✅ `findUserByEmail()` - поиск по email
- ✅ `linkOAuthAccount()` - привязка OAuth к существующему пользователю

### 2. API Endpoints

📄 **`server/api/auth/google.get.ts`**
- ✅ GET `/api/auth/google` - начало OAuth flow
- ✅ Генерация state для CSRF protection
- ✅ Сохранение state в HTTPOnly cookie
- ✅ Редирект на Google с правильными параметрами

📄 **`server/api/auth/callback/google.get.ts`**
- ✅ GET `/api/auth/callback/google` - обработка callback
- ✅ Проверка state (CSRF protection)
- ✅ Обмен code на токены
- ✅ Получение информации о пользователе
- ✅ Проверка verified_email
- ✅ Создание/обновление пользователя в БД
- ✅ Создание сессии
- ✅ Установка HTTPOnly cookie
- ✅ Редирект на главную

### 3. Frontend Components

📄 **`components/GoogleSignInButton.vue`**
- ✅ Красивая кнопка с Google лого
- ✅ Loading состояние
- ✅ Правильные стили (Google brand guidelines)

📄 **`pages/login.vue`**
- ✅ Универсальная страница входа
- ✅ Кнопка "Войти через Google"
- ✅ Ссылка на admin login
- ✅ Отображение информации о залогиненном пользователе
- ✅ Кнопка выхода
- ✅ Обработка OAuth ошибок
- ✅ Красивый UI

### 4. Documentation

📄 **`docs/GOOGLE_OAUTH_SETUP.md`**
- ✅ Пошаговая инструкция по настройке Google Cloud Console
- ✅ Создание OAuth Client ID
- ✅ Настройка redirect URIs
- ✅ Настройка переменных окружения
- ✅ Troubleshooting
- ✅ Проверка в БД

📄 **`.env.example`**
- ✅ Пример переменных окружения
- ✅ GOOGLE_CLIENT_ID
- ✅ GOOGLE_CLIENT_SECRET
- ✅ BASE_URL

## OAuth Flow

```
1. Пользователь → /login
2. Клик "Войти через Google"
3. Редирект → /api/auth/google
4. Генерация state, сохранение в cookie
5. Редирект → Google OAuth (accounts.google.com)
6. Пользователь авторизуется в Google
7. Google редиректит → /api/auth/callback/google?code=XXX&state=YYY
8. Проверка state (CSRF protection)
9. Обмен code → access_token + refresh_token
10. Получение user info (email, name, picture)
11. Проверка/создание пользователя в БД
    - Если новый: INSERT в users + oauth_accounts
    - Если существует: UPDATE токенов
12. Создание сессии в БД
13. Установка session_id cookie
14. Редирект → / (главная страница)
```

## Настройка Google OAuth

### Шаг 1: Google Cloud Console

1. Откройте https://console.cloud.google.com/
2. Создайте проект
3. APIs & Services → Credentials
4. OAuth consent screen:
   - App name: docta.me
   - Scopes: openid, email, profile
5. Create OAuth Client ID:
   - Type: Web application
   - Authorized redirect URIs:
     - `http://localhost:3000/api/auth/callback/google`
     - `https://docta.me/api/auth/callback/google`

### Шаг 2: Переменные окружения

Создайте `.env`:

```bash
GOOGLE_CLIENT_ID=ваш-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=ваш-client-secret
BASE_URL=http://localhost:3000
```

### Шаг 3: Запуск

```bash
npm run dev
```

Откройте http://localhost:3000/login

## Критерии приемки

- [x] AC-1: Google OAuth работает: можно войти и создается пользователь в БД
- [x] AC-2: При повторном входе через Google используется существующий пользователь
- [x] AC-3: Сессия сохраняется в БД и работает корректно
- [x] AC-4: После входа пользователь редиректится на главную страницу
- [x] AC-5: Данные пользователя (email, name, photo) корректно сохраняются
- [x] AC-6: В таблице oauth_accounts создается запись с provider='google'
- [x] AC-7: OAuth пользователи имеют is_admin=false и password_hash=NULL

## Тестирование

### Тест 1: Первый вход через Google

```bash
# 1. Откройте /login
# 2. Нажмите "Войти через Google"
# 3. Авторизуйтесь в Google
# 4. Проверьте БД:

SELECT * FROM users WHERE email = 'your-google-email@gmail.com';
-- Ожидается: is_admin=0, password_hash=NULL

SELECT * FROM oauth_accounts WHERE provider = 'google';
-- Ожидается: запись с вашим provider_account_id

SELECT * FROM sessions;
-- Ожидается: активная сессия
```

### Тест 2: Повторный вход

```bash
# 1. Выйдите (logout)
# 2. Снова войдите через Google
# 3. Проверьте БД:

SELECT COUNT(*) FROM users WHERE email = 'your-email@gmail.com';
-- Ожидается: 1 (не создался дубликат)

SELECT COUNT(*) FROM oauth_accounts WHERE provider = 'google';
-- Ожидается: 1 (не создался дубликат)
```

### Тест 3: Обновление токенов

```bash
# 1. Войдите через Google
# 2. Проверьте access_token:

SELECT access_token FROM oauth_accounts WHERE provider = 'google';

# 3. Выйдите и войдите снова
# 4. Проверьте что токен обновился (изменился)
```

### Тест 4: Проверка данных пользователя

```bash
SELECT id, email, name, photo_url, is_admin, password_hash
FROM users
WHERE id IN (SELECT user_id FROM oauth_accounts WHERE provider = 'google');

-- Проверьте:
-- ✓ email совпадает с Google
-- ✓ name совпадает с Google
-- ✓ photo_url - ссылка на Google аватар
-- ✓ is_admin = 0
-- ✓ password_hash = NULL
```

### Тест 5: CSRF Protection

```bash
# 1. Попробуйте открыть callback без state:
curl http://localhost:3000/api/auth/callback/google?code=test

# Ожидается: редирект на /?error=state_mismatch
```

### Тест 6: Ошибки OAuth

Проверьте обработку ошибок:
- Отмена авторизации в Google → `/?error=oauth_failed`
- Неподтвержденный email → `/?error=email_not_verified`

## Созданные файлы

```
server/
├── utils/
│   ├── oauth-config.ts ......................... ✅ Конфигурация OAuth
│   └── oauth.ts ................................ ✅ Утилиты для OAuth
└── api/
    └── auth/
        ├── google.get.ts ....................... ✅ Начало OAuth flow
        └── callback/
            └── google.get.ts ................... ✅ OAuth callback

components/
└── GoogleSignInButton.vue ...................... ✅ Кнопка входа

pages/
└── login.vue ................................... ✅ Страница входа

docs/
└── GOOGLE_OAUTH_SETUP.md ....................... ✅ Документация

.env.example .................................... ✅ Пример переменных
```

## Безопасность

✅ **CSRF Protection** - state parameter  
✅ **HTTPOnly cookies** - защита от XSS  
✅ **State validation** - проверка в callback  
✅ **Email verification check** - только подтвержденные email  
✅ **Token storage** - access/refresh tokens в БД  
✅ **Secure cookies in production** - HTTPS only  
✅ **Session expiration** - 30 дней

## Архитектурные решения

### Почему без библиотек?

✅ **Полный контроль** над процессом  
✅ **Меньше зависимостей** (next-auth не совсем для Nuxt)  
✅ **Гибкость** - легко добавить другие провайдеры  
✅ **Прозрачность** - понятно что происходит  
✅ **Легкая отладка**

### OAuth vs Email/Password

| Функция | OAuth (Google) | Email/Password (Admin) |
|---------|----------------|------------------------|
| Регистрация | Автоматическая | Ручная в БД |
| password_hash | NULL | Bcrypt hash |
| is_admin | FALSE | TRUE |
| photo_url | Google avatar | NULL |
| Таблица | users + oauth_accounts | users |

## Следующие шаги

✅ **Итерация 1 завершена** - БД готова  
✅ **Итерация 2 завершена** - Админ авторизация работает  
✅ **Итерация 3 завершена** - Google OAuth работает

⏭️ **Следующая итерация (опционально):** [Итерация 4 - OAuth через Telegram](../iterations/iteration-04-oauth-telegram.md)

Что будет реализовано:
- OAuth через Telegram для обычных пользователей
- Telegram Mini App integration (опционально)
- Привязка нескольких провайдеров к одному аккаунту

---

**Прогресс:** 60% (3/5 итераций) ██████░░░░

**Детали:** См. `prd/auth/PROGRESS.md`

## Важные замечания

⚠️ **Настройте Google Cloud Console!** (см. `docs/GOOGLE_OAUTH_SETUP.md`)  
⚠️ **Не коммитьте .env файл в git!**  
⚠️ **В production используйте HTTPS**  
⚠️ **Добавьте тестовых пользователей** (для External apps)

🎉 **OAuth через Google готов!**
