# Итерация 6: Продакшн и финализация

[← К списку итераций](README.md) | [Предыдущая](iteration-05-detail-pages.md)

---

## Цель

Запустить все тесты на продакшен окружении (https://docta.me), убедиться в их стабильности, настроить отчетность и создать документацию.

## Зависимости

- Все предыдущие итерации (1-5) должны быть завершены

## Задачи

1. Запустить тесты на продакшене и исправить проблемы
2. Настроить retry механизм
3. Настроить скриншоты и видео при падении
4. Создать документацию (README)
5. (Опционально) Настроить CI/CD интеграцию
6. Финальная проверка всех критериев

## Технические детали

### 1. Запуск тестов на продакшене

```bash
# Запустить все тесты на проде
npm run test:e2e:prod

# Запустить конкретный тест на проде
BASE_URL=https://docta.me npm run test:e2e -- clinics.spec.ts
```

**Возможные проблемы и решения:**

1. **Медленная загрузка:** Увеличить таймауты для продакшена
2. **Различия в селекторах:** Убедиться что селекторы работают на обоих окружениях
3. **Сетевые задержки:** Добавить больше ожиданий `waitForLoadState`

### 2. Улучшить конфигурацию

Обновить `playwright.config.ts`:

```typescript
export default defineConfig({
	// ... существующая конфигурация

	// Увеличить таймаут для продакшена
	timeout: baseURL.includes('docta.me') ? 60 * 1000 : 30 * 1000,

	// Retry для стабильности
	retries: process.env.CI ? 2 : 1,

	use: {
		baseURL,

		// Скриншоты при падении
		screenshot: 'only-on-failure',

		// Видео при первом retry
		video: 'retain-on-failure',

		// Трейсы для отладки
		trace: 'on-first-retry',

		// Увеличить таймаут для продакшена
		navigationTimeout: baseURL.includes('docta.me') ? 30000 : 10000,
		actionTimeout: baseURL.includes('docta.me') ? 15000 : 5000,
	},

	// Дополнительная конфигурация отчетов
	reporter: [
		[
			'html',
			{
				outputFolder: 'playwright-report',
				open: 'never', // Не открывать автоматически в CI
			},
		],
		['list'],
		['json', { outputFile: 'test-results/results.json' }],
	],
});
```

### 3. Создать файл: `tests/README.md`

````markdown
# E2E тесты для docta.me

Автоматические E2E тесты на Playwright для проверки основной функциональности сайта docta.me.

## Установка

```bash
# Установить зависимости
npm install

# Установить браузеры Playwright
npx playwright install
```
````

## Запуск тестов

### Локальное окружение

```bash
# Запустить dev сервер (в отдельном терминале)
npm run dev

# Запустить все тесты
npm run test:e2e

# Запустить конкретный тест
npm run test:e2e -- clinics.spec.ts

# Запустить с UI (интерактивный режим)
npm run test:e2e:ui

# Запустить с видимым браузером
npm run test:e2e:headed

# Отладка
npm run test:e2e:debug
```

### Продакшн окружение

```bash
# Запустить все тесты на проде
npm run test:e2e:prod

# Запустить конкретный тест на проде
BASE_URL=https://docta.me npm run test:e2e -- home.spec.ts
```

## Отчеты

```bash
# Посмотреть HTML отчет
npm run test:e2e:report
```

Отчеты сохраняются в:

- `playwright-report/` - HTML отчет
- `test-results/` - скриншоты, видео, трейсы

## Структура тестов

```
tests/
├── e2e/              # Тесты
│   ├── home.spec.ts
│   ├── navigation.spec.ts
│   ├── clinics.spec.ts
│   ├── doctors.spec.ts
│   ├── services.spec.ts
│   ├── medicines.spec.ts
│   └── articles.spec.ts
│
├── pages/            # Page Object Model
│   ├── base.page.ts
│   ├── home.page.ts
│   ├── components/
│   ├── lists/
│   └── details/
│
└── utils/            # Утилиты
    ├── constants.ts
    └── test-helpers.ts
```

## Покрытие

- ✅ Главная страница
- ✅ Навигация (хедер, футер)
- ✅ Переключение языка (SR/EN)
- ✅ Списки: клиники, врачи, услуги, лекарства, статьи
- ✅ Пагинация на всех списках
- ✅ Детальные страницы всех типов
- ✅ Кнопка "К поиску" на детальных страницах

**Общее количество тестов:** 50+  
**Покрыто страниц:** 13

## Troubleshooting

### Тесты падают локально

1. Убедитесь что dev сервер запущен (`npm run dev`)
2. Проверьте что порт 3000 свободен
3. Очистите кеш: `rm -rf .nuxt node_modules/.cache`

### Тесты падают на проде

1. Проверьте что сайт доступен: `curl https://docta.me`
2. Увеличьте таймауты в конфигурации
3. Проверьте скриншоты в `test-results/`

### Флаки-тесты

1. Добавьте больше ожиданий (`waitForLoadState`)
2. Используйте более стабильные селекторы
3. Увеличьте количество retry

## CI/CD

### GitHub Actions (пример)

См. `.github/workflows/e2e-tests.yml`

Тесты автоматически запускаются:

- При push в main
- При создании pull request
- По расписанию (каждый день в 6:00)

## Поддержка

При возникновении проблем:

1. Проверьте логи в `test-results/`
2. Посмотрите скриншоты и видео
3. Запустите с `--debug` флагом
4. Откройте issue с описанием проблемы

````

### 4. (Опционально) Создать файл: `.github/workflows/e2e-tests.yml`

```yaml
name: E2E Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  schedule:
    # Запускать каждый день в 6:00 UTC
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  test-local:
    name: E2E Tests (Local)
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run E2E tests (localhost)
        run: npm run test:e2e
        env:
          CI: true

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-local
          path: playwright-report/
          retention-days: 7

      - name: Upload test artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-local
          path: test-results/
          retention-days: 7

  test-production:
    name: E2E Tests (Production)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    # Запускать только на main и по расписанию
    if: github.ref == 'refs/heads/main' || github.event_name == 'schedule'

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run E2E tests (production)
        run: npm run test:e2e:prod
        env:
          CI: true

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-production
          path: playwright-report/
          retention-days: 7

      - name: Upload test artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-production
          path: test-results/
          retention-days: 7
````

### 5. Создать файл: `tests/.eslintrc.json` (опционально)

```json
{
	"extends": ["plugin:playwright/recommended"],
	"rules": {
		"playwright/no-wait-for-timeout": "warn",
		"playwright/no-element-handle": "error"
	}
}
```

## Критерии приемки

- [ ] AC-1: Все тесты проходят на localhost:3000
- [ ] AC-2: Все тесты проходят на https://docta.me
- [ ] AC-3: Retry механизм настроен (2 попытки при падении)
- [ ] AC-4: Скриншоты сохраняются при падении тестов
- [ ] AC-5: Видео записывается при первом retry
- [ ] AC-6: HTML отчеты генерируются корректно
- [ ] AC-7: Документация создана (`tests/README.md`)
- [ ] AC-8: Flakiness rate < 5% (тесты стабильны)
- [ ] AC-9: Время выполнения всех тестов < 5 минут (локально)
- [ ] AC-10: (Опционально) CI/CD настроен и работает

## Как проверить

### 1. Финальная проверка на localhost

```bash
# Запустить все тесты 3 раза подряд
for i in {1..3}; do
  echo "Run $i/3"
  npm run test:e2e
done
```

Ожидается:

- Все 3 прогона проходят успешно
- Нет флаки-тестов
- Время выполнения < 5 минут

### 2. Проверка на продакшене

```bash
# Запустить все тесты на проде
npm run test:e2e:prod
```

Ожидается:

- Все тесты проходят
- Возможно увеличенное время выполнения (< 10 минут)

### 3. Проверка отчетности

```bash
# Искусственно создать падающий тест
# Изменить любой тест чтобы он упал

npm run test:e2e

# Проверить что созданы:
ls test-results/         # Должны быть скриншоты
ls playwright-report/    # Должен быть HTML отчет

npm run test:e2e:report  # Открыть отчет
```

В отчете должны быть:

- Скриншот момента падения
- Стек трейс ошибки
- Информация о браузере

### 4. Проверка документации

```bash
cat tests/README.md
```

Документация должна быть:

- Полной (установка, запуск, troubleshooting)
- Понятной (новый разработчик может запустить тесты)
- Актуальной (все команды работают)

### 5. (Опционально) Проверка CI/CD

1. Создать pull request
2. Проверить что workflow запустился
3. Проверить что тесты прошли
4. Проверить артефакты (reports)

### 6. Финальная проверка метрик

Запустить все тесты и проверить:

```bash
npm run test:e2e
```

- ✅ Количество тестов: 50+
- ✅ Все тесты проходят: 100%
- ✅ Время выполнения: < 5 минут
- ✅ Flakiness rate: 0% (3 прогона подряд)

## Статус

**Not Started**

---

**🎉 После завершения этой итерации:** Все E2E тесты готовы к использованию!

[← Вернуться к списку итераций](README.md) | [📊 Обновить прогресс](../PROGRESS.md)
