# 1. Обзор

[← Назад к оглавлению](index.md)

---

## Цель

Унифицировать доступ к переменным окружения в проекте, заменив использование `runtimeConfig` из Nuxt 3 на прямое использование `process.env` с dotenv.

---

## Контекст

### Текущая ситуация

В проекте существует **смешанный подход** к работе с переменными окружения:

1. **runtimeConfig в nuxt.config.ts** - дублирует переменные из process.env в публичный runtimeConfig
2. **useRuntimeConfig()** - используется в 5 файлах для доступа к переменным
3. **process.env** - используется напрямую в 24 файлах

### Проблемы текущего подхода

#### 1. Неконсистентность

```typescript
// В одних файлах:
const config = useRuntimeConfig();
const token = config.public.mixpanelToken;

// В других файлах:
const token = process.env.MIXPANEL_TOKEN;
```

Непонятно, какой подход использовать, когда и где.

#### 2. Ограниченная доступность

`useRuntimeConfig()` работает только в Nuxt context:

- ✅ Работает: API routes, server utils с event context, composables
- ❌ Не работает: Standalone utils, middleware без context, build scripts

Пример проблемы:

```typescript
// server/utils/db-mysql.ts
export async function getConnection() {
	const config = useRuntimeConfig(); // ❌ Может не работать в некоторых контекстах
	const { host, user, password } = config.public.connection;
	// ...
}
```

#### 3. Дублирование

Переменные дублируются в `nuxt.config.ts`:

```typescript
runtimeConfig: {
  public: {
    mixpanelToken: process.env.MIXPANEL_TOKEN, // ❌ Дублирование
    cloudflareToken: process.env.CLOUDFLARE_TOKEN, // ❌ Дублирование
    telegramBotUsername: process.env.TELEGRAM_BOT_USERNAME, // ❌ Дублирование
    connection: {
      host: process.env.DB_HOST, // ❌ Дублирование
      user: process.env.DB_USER, // ❌ Дублирование
      password: process.env.DB_PASSWORD, // ❌ Дублирование
    },
  },
},
```

#### 4. Проблемы с типизацией

Нет единого места для определения типов переменных окружения. TypeScript не знает, какие переменные доступны.

#### 5. Путаница в команде

Разработчики не знают:

- Когда использовать `runtimeConfig`, а когда `process.env`
- Зачем нужно дублировать переменные в `nuxt.config.ts`
- Почему в одних файлах работает один подход, а в других - другой

---

## Решение

### Подход

Использовать **стандартный Node.js подход** с `process.env` + dotenv:

1. **Один способ доступа** - `process.env` везде
2. **Типизация** - TypeScript types для всех переменных
3. **Валидация** - проверка обязательных переменных при старте
4. **Простота** - нет необходимости в runtimeConfig

### Преимущества

✅ **Единообразие** - один способ доступа ко всем переменным  
✅ **Доступность** - работает везде, без привязки к Nuxt context  
✅ **Простота** - нет дублирования, прямой доступ  
✅ **Типизация** - TypeScript знает о всех переменных  
✅ **Валидация** - ошибки при отсутствии обязательных переменных  
✅ **Стандартность** - стандартный подход Node.js, понятный всем

### Как будет выглядеть

#### До миграции

```typescript
// nuxt.config.ts
runtimeConfig: {
  public: {
    mixpanelToken: process.env.MIXPANEL_TOKEN,
  },
},

// composables/use-analytics.ts
const config = useRuntimeConfig();
const token = config.public.mixpanelToken;
```

#### После миграции

```typescript
// nuxt.config.ts
// (секция runtimeConfig удалена)

// composables/use-analytics.ts
const token = process.env.MIXPANEL_TOKEN;
```

---

## Предположения

1. **dotenv уже установлен** - Nuxt 3 из коробки поддерживает .env файлы
2. **.env.example существует** - уже есть файл с примерами переменных
3. **Переменные не меняются в runtime** - все env переменные статичны
4. **Нет SSR-специфичных требований** - не используются серверные/клиентские env переменные отдельно

---

## Ограничения

### Что НЕ входит в scope

- ❌ Изменение самих переменных окружения (имена, значения)
- ❌ Добавление новых переменных
- ❌ Изменение логики работы с переменными
- ❌ Миграция на другие системы конфигурации (config files, etc)

### Что остается как есть

- ✅ `.env.example` - остается как референс
- ✅ Имена переменных - остаются прежними
- ✅ Значения переменных - не меняются
- ✅ Логика приложения - не меняется

---

## Целевая аудитория

### Разработчики

- Единообразный доступ к env переменным
- Понятная типизация
- Меньше путаницы

### DevOps

- Нет изменений в деплое
- Те же переменные окружения

---

## Ценность

### Для разработчиков

- 🎯 **Меньше когнитивной нагрузки** - один способ вместо двух
- 🎯 **Меньше ошибок** - типизация и валидация
- 🎯 **Быстрее разработка** - не нужно думать, какой подход использовать

### Для проекта

- 🎯 **Упрощение кодовой базы** - меньше абстракций
- 🎯 **Улучшение поддерживаемости** - понятнее новым разработчикам
- 🎯 **Стандартизация** - следование best practices Node.js

---

[← Назад к оглавлению](index.md) | [Далее: Требования →](02-requirements.md)
