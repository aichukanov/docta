# Итерации разработки

[← Назад к оглавлению](../index.md)

---

## Обзор итераций

Миграция с `runtimeConfig` на `process.env` разделена на **4 итерации**:

| #   | Название                  | Статус         | Файлов | Прогресс |
| --- | ------------------------- | -------------- | ------ | -------- |
| 1   | Аудит и подготовка        | 🔴 Not Started | 2      | 0%       |
| 2   | Миграция server-side кода | 🔴 Not Started | 3      | 0%       |
| 3   | Миграция client-side кода | 🔴 Not Started | 2      | 0%       |
| 4   | Очистка и документация    | 🔴 Not Started | 5+     | 0%       |

---

## Итерация 1: Аудит и подготовка

**Цель:** Подготовить инфраструктуру для миграции

**Задачи:**

- Провести полный аудит использования `useRuntimeConfig`
- Создать типы для переменных окружения (`types/env.d.ts`)
- Создать утилиту валидации (`server/utils/validate-env.ts`)
- Составить план миграции

**Детали:** [iteration-01-audit.md](iteration-01-audit.md)

---

## Итерация 2: Миграция server-side кода

**Цель:** Мигрировать серверную часть на `process.env`

**Задачи:**

- Мигрировать `server/common/db-mysql.ts`
- Мигрировать другие server utils (если используют runtimeConfig)
- Добавить валидацию env при старте сервера
- Протестировать подключение к БД и другие server-side функции

**Детали:** [iteration-02-server-migration.md](iteration-02-server-migration.md)

---

## Итерация 3: Миграция client-side кода

**Цель:** Мигрировать клиентскую часть на `process.env` с `NUXT_PUBLIC_` префиксом

**Задачи:**

- Переименовать публичные env переменные с префиксом `NUXT_PUBLIC_`
- Мигрировать `composables/use-analytics.ts`
- Мигрировать `components/TelegramLoginButton.vue` (если нужно)
- Протестировать аналитику и OAuth

**Детали:** [iteration-03-client-migration.md](iteration-03-client-migration.md)

---

## Итерация 4: Очистка и документация

**Цель:** Завершить миграцию, очистить код, обновить документацию

**Задачи:**

- Удалить секцию `runtimeConfig.public` из `nuxt.config.ts`
- Проверить отсутствие `useRuntimeConfig` в кодовой базе
- Обновить `.env.example`
- Обновить документацию (README, docs/)
- Создать migration guide для команды
- Финальное тестирование

**Детали:** [iteration-04-cleanup.md](iteration-04-cleanup.md)

---

## Зависимости между итерациями

```
Итерация 1 (Аудит и подготовка)
    ↓
Итерация 2 (Server-side)
    ↓
Итерация 3 (Client-side)
    ↓
Итерация 4 (Очистка и документация)
```

**Важно:** Итерации должны выполняться **последовательно**. Нельзя начать следующую итерацию, не завершив предыдущую.

---

## Общие правила для всех итераций

### Перед началом итерации

- [ ] Создать feature branch от `master`
- [ ] Убедиться, что `master` не имеет неразрешенных конфликтов
- [ ] Прочитать итерацию целиком перед началом

### Во время итерации

- [ ] Делать коммиты после каждой значимой задачи
- [ ] Тестировать изменения сразу после внесения
- [ ] Документировать нестандартные решения

### После завершения итерации

- [ ] Проверить все Acceptance Criteria
- [ ] Выполнить self code review
- [ ] Протестировать вручную
- [ ] Создать PR и запросить review
- [ ] После апрува - замержить в `master`

---

## Откат изменений

Если что-то пошло не так на любой итерации:

```bash
# Откат последнего коммита
git revert HEAD

# Откат всех изменений в ветке
git reset --hard origin/master

# Откат конкретного файла
git checkout origin/master -- path/to/file.ts
```

---

## Контакты и вопросы

Если возникли вопросы по итерациям:

- Прочитать секцию "Troubleshooting" в конце каждой итерации
- Проверить [04-risks-and-metrics.md](../04-risks-and-metrics.md)
- Проверить [03-architecture.md](../03-architecture.md) для примеров

---

[← Назад к оглавлению](../index.md)
