# Итерация 1: Аудит и подготовка

[← Назад к итерациям](README.md) | [← Назад к оглавлению](../index.md)

---

## Информация об итерации

**Статус:** 🔴 Not Started  
**Приоритет:** P0 (критично)  
**Оценка времени:** 2-3 часа

---

## Цель

Подготовить инфраструктуру для безопасной миграции с `runtimeConfig` на `process.env`:

- Провести полный аудит использования `useRuntimeConfig`
- Создать TypeScript типы для всех переменных окружения
- Создать утилиту для валидации обязательных переменных
- Составить детальный план миграции

---

## Предварительные условия

- ✅ Проект использует Nuxt 3
- ✅ Есть файл `.env` с переменными окружения
- ✅ Есть файл `.env.example` с примерами переменных

---

## Зависимости

**От чего зависит:**

- Ничего (первая итерация)

**Что зависит от этой итерации:**

- Итерация 2 (миграция server-side)
- Итерация 3 (миграция client-side)

---

## Задачи

### Задача 1.1: Аудит использования useRuntimeConfig

**Цель:** Найти все места, где используется `useRuntimeConfig()`

**Шаги:**

1. Выполнить поиск по кодовой базе:

```bash
# Найти все файлы с useRuntimeConfig
grep -r "useRuntimeConfig" --include="*.ts" --include="*.vue" --include="*.js" . > audit-runtimeconfig.txt

# Посчитать количество использований
grep -r "useRuntimeConfig" --include="*.ts" --include="*.vue" --include="*.js" . | wc -l
```

2. Проанализировать найденные файлы:

   - Определить, где используется на сервере (server/)
   - Определить, где используется на клиенте (composables/, components/)
   - Определить, какие переменные используются

3. Составить список файлов для миграции:

**Ожидаемый результат:**

```
Файлы для миграции:

Server-side:
1. server/common/db-mysql.ts
   - DB_HOST
   - DB_USER
   - DB_PASSWORD

2. server/utils/email.ts (если используется)
   - SMTP_HOST
   - SMTP_PORT
   - и т.д.

Client-side:
1. composables/use-analytics.ts
   - MIXPANEL_TOKEN
   - CLOUDFLARE_TOKEN

2. components/TelegramLoginButton.vue
   - TELEGRAM_BOT_USERNAME
```

**AC-1.1:**

- ✅ Создан файл `audit-runtimeconfig.txt` со списком всех использований
- ✅ Создан список файлов для миграции с указанием используемых переменных

---

### Задача 1.2: Создать типы для переменных окружения

**Цель:** Создать TypeScript типы для всех переменных окружения

**Шаги:**

1. Изучить `.env.example` и найти все используемые переменные

2. Создать файл `types/env.d.ts`:

```typescript
declare global {
	namespace NodeJS {
		interface ProcessEnv {
			// Node environment
			NODE_ENV: 'development' | 'production' | 'test';

			// Database (обязательные)
			DB_HOST: string;
			DB_USER: string;
			DB_PASSWORD: string;

			// Analytics (опциональные, публичные)
			NUXT_PUBLIC_MIXPANEL_TOKEN?: string;
			NUXT_PUBLIC_CLOUDFLARE_TOKEN?: string;
			NUXT_PUBLIC_GTAG_ID?: string;

			// OAuth (опциональные)
			GOOGLE_CLIENT_ID?: string;
			GOOGLE_CLIENT_SECRET?: string;
			TELEGRAM_BOT_TOKEN?: string;
			NUXT_PUBLIC_TELEGRAM_BOT_USERNAME?: string;
			FACEBOOK_APP_ID?: string;
			FACEBOOK_APP_SECRET?: string;

			// Email (обязательные)
			SMTP_HOST: string;
			SMTP_PORT: string;
			SMTP_USER: string;
			SMTP_PASSWORD: string;
			SMTP_FROM: string;

			// Application
			NUXT_PUBLIC_BASE_URL?: string;
			PORT?: string;
		}
	}
}

export {};
```

3. Проверить, что TypeScript распознает типы:

```typescript
// Должен работать автокомплит
const host = process.env.DB_HOST; // TypeScript знает тип: string
const token = process.env.NUXT_PUBLIC_MIXPANEL_TOKEN; // string | undefined
```

**AC-1.2:**

- ✅ Создан файл `types/env.d.ts`
- ✅ Все используемые переменные типизированы
- ✅ Разделены обязательные (string) и опциональные (string | undefined)
- ✅ Публичные переменные имеют префикс `NUXT_PUBLIC_`
- ✅ TypeScript автокомплит работает для `process.env.*`

---

### Задача 1.3: Создать утилиту валидации env

**Цель:** Создать функцию для валидации обязательных переменных при старте

**Шаги:**

1. Создать файл `server/utils/validate-env.ts`:

```typescript
/**
 * Валидация обязательных переменных окружения
 * Вызывается при старте приложения
 *
 * @throws {Error} Если отсутствуют или пусты обязательные переменные
 */
export function validateEnv() {
	const required = [
		'DB_HOST',
		'DB_USER',
		'DB_PASSWORD',
		'SMTP_HOST',
		'SMTP_PORT',
		'SMTP_USER',
		'SMTP_PASSWORD',
		'SMTP_FROM',
	];

	const missing: string[] = [];
	const empty: string[] = [];

	for (const key of required) {
		if (!(key in process.env)) {
			missing.push(key);
		} else if (!process.env[key]) {
			empty.push(key);
		}
	}

	if (missing.length > 0 || empty.length > 0) {
		const errors: string[] = [];

		if (missing.length > 0) {
			errors.push(`Missing variables: ${missing.join(', ')}`);
		}

		if (empty.length > 0) {
			errors.push(`Empty variables: ${empty.join(', ')}`);
		}

		throw new Error(
			`❌ Environment validation failed:\n${errors.join('\n')}\n\n` +
				`Please check your .env file and ensure all required variables are set.`,
		);
	}

	console.log('✅ Environment variables validated successfully');
}
```

2. Проверить работу утилиты (пока без интеграции):

```typescript
// Временный тестовый файл для проверки
import { validateEnv } from './server/utils/validate-env';

try {
	validateEnv();
	console.log('Validation passed!');
} catch (error) {
	console.error(error.message);
}
```

**AC-1.3:**

- ✅ Создан файл `server/utils/validate-env.ts`
- ✅ Функция проверяет наличие обязательных переменных
- ✅ Функция выбрасывает ошибку с понятным сообщением
- ✅ Функция выводит ✅ при успешной валидации
- ✅ Функция протестирована вручную

**Примечание:** Интеграция в приложение будет в Итерации 2.

---

### Задача 1.4: Составить план миграции

**Цель:** Создать детальный план для следующих итераций

**Шаги:**

1. На основе аудита из Задачи 1.1 составить список файлов для миграции

2. Создать файл `prd/env-config/MIGRATION_PLAN.md`:

```markdown
# План миграции с runtimeConfig на process.env

## Итерация 2: Server-side

### Файлы для миграции

1. **server/common/db-mysql.ts**

   - Убрать: `useRuntimeConfig()`
   - Заменить: `config.public.connection.host` → `process.env.DB_HOST`
   - Заменить: `config.public.connection.user` → `process.env.DB_USER`
   - Заменить: `config.public.connection.password` → `process.env.DB_PASSWORD`
   - Тестирование: проверить подключение к БД

2. **server/utils/email.ts** (если используется)
   - Убрать: `useRuntimeConfig()`
   - Заменить на прямой доступ к SMTP\_\* переменным
   - Тестирование: отправить тестовый email

### Интеграция валидации

- Создать `server/plugins/init.ts`
- Вызвать `validateEnv()` при старте сервера

## Итерация 3: Client-side

### Переименование переменных

Переименовать в `.env`:

- `MIXPANEL_TOKEN` → `NUXT_PUBLIC_MIXPANEL_TOKEN`
- `CLOUDFLARE_TOKEN` → `NUXT_PUBLIC_CLOUDFLARE_TOKEN`
- `TELEGRAM_BOT_USERNAME` → `NUXT_PUBLIC_TELEGRAM_BOT_USERNAME`

### Файлы для миграции

1. **composables/use-analytics.ts**

   - Убрать: `useRuntimeConfig()`
   - Заменить: `config.public.mixpanelToken` → `process.env.NUXT_PUBLIC_MIXPANEL_TOKEN`
   - Заменить: `config.public.cloudflareToken` → `process.env.NUXT_PUBLIC_CLOUDFLARE_TOKEN`
   - Тестирование: проверить инициализацию Mixpanel и Cloudflare

2. **components/TelegramLoginButton.vue**
   - Убрать: `useRuntimeConfig()`
   - Заменить: `config.public.telegramBotUsername` → `process.env.NUXT_PUBLIC_TELEGRAM_BOT_USERNAME`
   - Тестирование: проверить Telegram OAuth

## Итерация 4: Очистка

- Удалить `runtimeConfig.public` из `nuxt.config.ts`
- Проверить отсутствие `useRuntimeConfig` в коде
- Обновить `.env.example`
- Обновить документацию
```

**AC-1.4:**

- ✅ Создан файл `MIGRATION_PLAN.md` с детальным планом
- ✅ План включает все файлы для миграции
- ✅ План включает конкретные изменения для каждого файла
- ✅ План включает тесты для каждого изменения

---

## Acceptance Criteria (общие для итерации)

- ✅ **AC-1.1:** Проведен аудит, создан список файлов для миграции
- ✅ **AC-1.2:** Создан `types/env.d.ts` с типами всех переменных
- ✅ **AC-1.3:** Создана утилита `server/utils/validate-env.ts`
- ✅ **AC-1.4:** Создан `MIGRATION_PLAN.md` с детальным планом
- ✅ TypeScript компилируется без ошибок
- ✅ Ничего не сломано (только добавлены новые файлы)

---

## Проверка итерации

### Checklist

**Код:**

- [ ] Создан `types/env.d.ts`
- [ ] Создан `server/utils/validate-env.ts`
- [ ] Создан `MIGRATION_PLAN.md`
- [ ] Создан `audit-runtimeconfig.txt`
- [ ] TypeScript компилируется: `npm run build`

**Функциональность:**

- [ ] Приложение запускается: `npm run dev`
- [ ] Нет новых ошибок в консоли
- [ ] TypeScript автокомплит работает для `process.env.*`

**Документация:**

- [ ] План миграции понятен и детален
- [ ] Типы env переменных документированы

---

## Troubleshooting

### Проблема: TypeScript не видит типы из env.d.ts

**Решение:**

1. Проверить, что `types/env.d.ts` включен в `tsconfig.json`:

```json
{
	"include": ["types/**/*"]
}
```

2. Перезапустить TypeScript сервер в IDE

### Проблема: validateEnv выбрасывает ошибки

**Решение:**

1. Проверить, что все переменные есть в `.env`
2. Проверить, что `.env` загружен (Nuxt автоматически загружает)
3. Проверить, что переменные не пусты

---

## Следующие шаги

После завершения этой итерации:

1. Проверить все Acceptance Criteria
2. Создать commit: `feat: add env types and validation utility`
3. Перейти к [Итерации 2: Миграция server-side кода](iteration-02-server-migration.md)

---

[← Назад к итерациям](README.md) | [Далее: Итерация 2 →](iteration-02-server-migration.md)
