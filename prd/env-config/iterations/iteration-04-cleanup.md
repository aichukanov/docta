# Итерация 4: Очистка и документация

[← Назад к итерациям](README.md) | [← Предыдущая: Итерация 3](iteration-03-client-migration.md)

---

## Информация об итерации

**Статус:** 🔴 Not Started  
**Приоритет:** P0 (критично)  
**Оценка времени:** 2-3 часа

---

## Цель

Завершить миграцию, удалив все следы `runtimeConfig` из проекта и обновив документацию.

**Результат:**

- ✅ Секция `runtimeConfig` удалена из `nuxt.config.ts`
- ✅ Нет использований `useRuntimeConfig` в коде
- ✅ Документация обновлена
- ✅ Создан migration guide
- ✅ Приложение полностью протестировано

---

## Предварительные условия

- ✅ Завершена [Итерация 3: Миграция client-side кода](iteration-03-client-migration.md)
- ✅ Server-side код мигрирован
- ✅ Client-side код мигрирован
- ✅ Все функции работают

---

## Зависимости

**От чего зависит:**

- Итерации 1, 2, 3 (весь код должен быть мигрирован)

**Что зависит от этой итерации:**

- Ничего (финальная итерация)

---

## Задачи

### Задача 4.1: Удалить runtimeConfig из nuxt.config.ts

**Цель:** Удалить секцию `runtimeConfig.public` из конфигурации Nuxt

**Текущий код (`nuxt.config.ts`):**

```typescript
export default defineNuxtConfig({
	// ... другие настройки ...

	runtimeConfig: {
		public: {
			mixpanelToken: process.env.MIXPANEL_TOKEN,
			cloudflareToken: process.env.CLOUDFLARE_TOKEN,
			telegramBotUsername: process.env.TELEGRAM_BOT_USERNAME,
			connection: {
				host: process.env.DB_HOST,
				user: process.env.DB_USER,
				password: process.env.DB_PASSWORD,
			},
		},
	}, // ❌ Удалить всю эту секцию

	// ... другие настройки ...
});
```

**Новый код:**

```typescript
export default defineNuxtConfig({
	// ... другие настройки ...
	// ✅ Секция runtimeConfig удалена
	// ... другие настройки ...
});
```

**Шаги:**

1. Открыть `nuxt.config.ts`
2. Найти секцию `runtimeConfig: { ... }`
3. Удалить всю секцию целиком
4. Сохранить файл
5. Перезапустить приложение: `npm run dev`
6. Убедиться, что приложение запускается без ошибок

**AC-4.1:**

- ✅ В `nuxt.config.ts` нет секции `runtimeConfig`
- ✅ Приложение запускается без ошибок
- ✅ TypeScript компилируется без ошибок

---

### Задача 4.2: Финальная проверка кодовой базы

**Цель:** Убедиться, что в коде не осталось использований `useRuntimeConfig`

**Шаги:**

1. Выполнить глобальный поиск `useRuntimeConfig`:

```bash
grep -r "useRuntimeConfig" . \
  --include="*.ts" \
  --include="*.vue" \
  --include="*.js" \
  --exclude-dir=node_modules \
  --exclude-dir=.nuxt \
  --exclude-dir=.output \
  --exclude-dir=prd
```

**Ожидаемый результат:** 0 совпадений

2. Выполнить поиск `runtimeConfig` в конфиге:

```bash
grep "runtimeConfig" nuxt.config.ts
```

**Ожидаемый результат:** 0 совпадений

3. Если найдены использования:
   - Определить, почему они не были мигрированы
   - Мигрировать согласно предыдущим итерациям
   - Повторить проверку

**AC-4.2:**

- ✅ Поиск `useRuntimeConfig` возвращает 0 результатов
- ✅ Поиск `runtimeConfig` в `nuxt.config.ts` возвращает 0 результатов
- ✅ Весь код мигрирован на `process.env`

---

### Задача 4.3: Обновить .env.example

**Цель:** Убедиться, что `.env.example` актуален после переименования переменных

**Шаги:**

1. Открыть `.env.example`

2. Проверить наличие всех переменных с правильными именами:

```bash
# Database (приватные - БЕЗ префикса)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password

# Email (приватные - БЕЗ префикса)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_email@example.com
SMTP_PASSWORD=your_password
SMTP_FROM=noreply@docta.me

# Analytics (публичные - С префиксом NUXT_PUBLIC_)
NUXT_PUBLIC_MIXPANEL_TOKEN=your_mixpanel_token
NUXT_PUBLIC_CLOUDFLARE_TOKEN=your_cloudflare_token
NUXT_PUBLIC_GTAG_ID=your_gtag_id

# OAuth secrets (приватные - БЕЗ префикса)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
FACEBOOK_APP_SECRET=your_facebook_app_secret

# OAuth public (публичные - С префиксом NUXT_PUBLIC_)
NUXT_PUBLIC_TELEGRAM_BOT_USERNAME=your_bot_username
NUXT_PUBLIC_FACEBOOK_APP_ID=your_facebook_app_id

# Application (публичные - С префиксом NUXT_PUBLIC_)
NUXT_PUBLIC_BASE_URL=https://docta.me
PORT=3000
```

3. Добавить комментарии для ясности:

```bash
# ===================================
# ПРИВАТНЫЕ ПЕРЕМЕННЫЕ (только server-side)
# ===================================

# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password

# Email
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_email@example.com
SMTP_PASSWORD=your_password
SMTP_FROM=noreply@docta.me

# OAuth secrets
GOOGLE_CLIENT_SECRET=your_google_client_secret
FACEBOOK_APP_SECRET=your_facebook_app_secret
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# ===================================
# ПУБЛИЧНЫЕ ПЕРЕМЕННЫЕ (client + server)
# Префикс NUXT_PUBLIC_ обязателен!
# ===================================

# Analytics
NUXT_PUBLIC_MIXPANEL_TOKEN=your_mixpanel_token
NUXT_PUBLIC_CLOUDFLARE_TOKEN=your_cloudflare_token
NUXT_PUBLIC_GTAG_ID=your_gtag_id

# OAuth public IDs
NUXT_PUBLIC_TELEGRAM_BOT_USERNAME=your_bot_username
NUXT_PUBLIC_FACEBOOK_APP_ID=your_facebook_app_id

# Application
NUXT_PUBLIC_BASE_URL=https://docta.me
PORT=3000
```

**AC-4.3:**

- ✅ `.env.example` содержит все используемые переменные
- ✅ Публичные переменные имеют префикс `NUXT_PUBLIC_`
- ✅ Приватные переменные БЕЗ префикса
- ✅ Добавлены комментарии для разделения групп

---

### Задача 4.4: Создать migration guide для команды

**Цель:** Документировать миграцию для команды

**Шаги:**

1. Создать файл `docs/ENV_MIGRATION.md`:

````markdown
# Миграция с runtimeConfig на process.env

**Дата миграции:** 2026-02-03  
**Статус:** ✅ Завершено

---

## Что изменилось

### До миграции

Использовали `useRuntimeConfig()` для доступа к переменным окружения:

\`\`\`typescript
const config = useRuntimeConfig();
const token = config.public.mixpanelToken;
\`\`\`

### После миграции

Используем `process.env` напрямую:

\`\`\`typescript
const token = process.env.NUXT_PUBLIC_MIXPANEL_TOKEN;
\`\`\`

---

## Изменения в переменных окружения

### Переименованные переменные

Публичные переменные теперь требуют префикс `NUXT_PUBLIC_`:

| Старое имя              | Новое имя                           |
| ----------------------- | ----------------------------------- |
| `MIXPANEL_TOKEN`        | `NUXT_PUBLIC_MIXPANEL_TOKEN`        |
| `CLOUDFLARE_TOKEN`      | `NUXT_PUBLIC_CLOUDFLARE_TOKEN`      |
| `TELEGRAM_BOT_USERNAME` | `NUXT_PUBLIC_TELEGRAM_BOT_USERNAME` |

### Приватные переменные (без изменений)

Остались без префикса:

- `DB_HOST`, `DB_USER`, `DB_PASSWORD`
- `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASSWORD`, `SMTP_FROM`
- `GOOGLE_CLIENT_SECRET`, `FACEBOOK_APP_SECRET`, `TELEGRAM_BOT_TOKEN`

---

## Как работать с env переменными теперь

### 1. Server-side код (API routes, server utils)

Используйте `process.env` напрямую:

\`\`\`typescript
// server/api/example.ts
export default defineEventHandler(async (event) => {
const dbHost = process.env.DB_HOST;
const mixpanelToken = process.env.NUXT_PUBLIC_MIXPANEL_TOKEN;
// ...
});
\`\`\`

### 2. Client-side код (composables, components)

Используйте только переменные с префиксом `NUXT_PUBLIC_`:

\`\`\`typescript
// composables/use-example.ts
export function useExample() {
const mixpanelToken = process.env.NUXT_PUBLIC_MIXPANEL_TOKEN; // ✅ Работает
const dbPassword = process.env.DB_PASSWORD; // ❌ undefined (безопасно!)
}
\`\`\`

### 3. Типизация

TypeScript автоматически подскажет доступные переменные благодаря `types/env.d.ts`.

---

## Обновление локального окружения

Если вы разработчик в команде:

1. Выполните `git pull` для получения последних изменений

2. Обновите ваш `.env` файл:
   \`\`\`bash

   # Переименуйте переменные

   mv .env .env.backup
   cp .env.example .env

   # Скопируйте значения из .env.backup в .env

   \`\`\`

3. Перезапустите dev сервер:
   \`\`\`bash
   npm run dev
   \`\`\`

---

## FAQ

### Почему мы мигрировали?

- ✅ Единообразие - один способ доступа к переменным
- ✅ Доступность - `process.env` работает везде
- ✅ Простота - нет дублирования в `nuxt.config.ts`

### Что делать, если нужна новая переменная?

1. Добавить в `.env`:

   - Если публичная (для клиента) - с префиксом `NUXT_PUBLIC_`
   - Если приватная (только сервер) - без префикса

2. Добавить в `.env.example`

3. Добавить в `types/env.d.ts` для TypeScript

4. Использовать в коде: `process.env.VARIABLE_NAME`

### Что если я вижу ошибку "undefined variable"?

1. Проверьте, что переменная есть в `.env`
2. Для client-side: проверьте наличие префикса `NUXT_PUBLIC_`
3. Перезапустите dev сервер

---

## Контакты

Вопросы по миграции: [ваш контакт]
\`\`\`

**AC-4.4:**

- ✅ Создан файл `docs/ENV_MIGRATION.md`
- ✅ Guide объясняет, что изменилось
- ✅ Guide объясняет, как работать с env теперь
- ✅ Guide включает FAQ
- ✅ Guide понятен для новых разработчиков

---

### Задача 4.5: Обновить документацию с примерами кода

**Цель:** Найти и обновить все примеры кода в документации, использующие `useRuntimeConfig`

**Шаги:**

1. Найти все упоминания `useRuntimeConfig` в документации:

```bash
grep -r "useRuntimeConfig" docs/ prd/ README.md --include="*.md"
```
````

2. Найти все упоминания `runtimeConfig` в документации:

```bash
grep -r "runtimeConfig" docs/ prd/ README.md --include="*.md"
```

3. Обновить найденные файлы:

   - Заменить примеры с `useRuntimeConfig()` на `process.env`
   - Обновить инструкции по настройке env переменных
   - Добавить информацию о префиксе `NUXT_PUBLIC_`

**Ожидаемые файлы для обновления:**

- `prd/analytics/03-architecture.md`
- `prd/analytics/iterations/iteration-01-setup.md`
- `docs/BASE_URL_CONFIG.md` (если упоминается)
- Любые другие файлы с примерами кода

**Пример обновления:**

**Было:**

```markdown
## Использование

\`\`\`typescript
const config = useRuntimeConfig();
const token = config.public.mixpanelToken;
\`\`\`
```

**Стало:**

```markdown
## Использование

\`\`\`typescript
const token = process.env.NUXT_PUBLIC_MIXPANEL_TOKEN;
\`\`\`
```

**AC-4.5:**

- ✅ Все упоминания `useRuntimeConfig` в документации обновлены
- ✅ Все примеры кода используют `process.env`
- ✅ Документация упоминает префикс `NUXT_PUBLIC_` для публичных переменных

---

### Задача 4.6: Финальное тестирование

**Цель:** Провести полное тестирование приложения после миграции

**Шаги:**

1. **Dev окружение:**

   ```bash
   npm run dev
   ```

   - [ ] Приложение запускается без ошибок
   - [ ] Валидация env показывает ✅
   - [ ] Нет ошибок в консоли сервера

2. **Production build:**

   ```bash
   npm run build
   npm run preview
   ```

   - [ ] Build проходит без ошибок
   - [ ] Приложение запускается в production режиме
   - [ ] Нет ошибок

3. **Функциональное тестирование:**

   - [ ] **База данных:** Открыть любую страницу с данными - данные загружаются
   - [ ] **API:** Протестировать 2-3 API endpoint - работают
   - [ ] **Mixpanel:** Открыть DevTools Console - Mixpanel инициализируется
   - [ ] **Cloudflare:** DevTools Network - скрипт загружается
   - [ ] **OAuth Google:** Кнопка "Войти через Google" работает
   - [ ] **OAuth Telegram:** Кнопка "Войти через Telegram" работает
   - [ ] **Email:** (опционально) Отправить тестовый email - работает

4. **TypeScript проверка:**

   ```bash
   npx tsc --noEmit
   ```

   - [ ] Нет TypeScript ошибок

5. **Проверка production bundle:**

   ```bash
   # Проверить, что секреты не попали в client bundle
   grep -r "DB_PASSWORD" .output/public/
   # Должно быть: 0 результатов
   ```

**AC-4.6:**

- ✅ Dev режим работает без ошибок
- ✅ Production build успешен
- ✅ Все функции работают как раньше
- ✅ TypeScript компилируется без ошибок
- ✅ Секреты НЕ попали в client bundle

---

## Acceptance Criteria (общие для итерации)

- ✅ **AC-4.1:** Секция `runtimeConfig` удалена из `nuxt.config.ts`
- ✅ **AC-4.2:** Нет использований `useRuntimeConfig` в коде
- ✅ **AC-4.3:** `.env.example` обновлен и актуален
- ✅ **AC-4.4:** Создан `docs/ENV_MIGRATION.md` guide
- ✅ **AC-4.5:** Документация обновлена (примеры кода)
- ✅ **AC-4.6:** Приложение полностью протестировано
- ✅ Все функции работают как раньше
- ✅ Проект готов к продакшену

---

## Проверка итерации

### Checklist

**Код:**

- [ ] `nuxt.config.ts` не содержит `runtimeConfig`
- [ ] Поиск `useRuntimeConfig` возвращает 0 результатов
- [ ] `.env.example` обновлен
- [ ] TypeScript компилируется: `npm run build`

**Документация:**

- [ ] Создан `docs/ENV_MIGRATION.md`
- [ ] Обновлены все файлы с примерами кода
- [ ] README обновлен (если нужно)

**Функциональность:**

- [ ] Dev сервер работает
- [ ] Production build успешен
- [ ] БД подключается
- [ ] API endpoints работают
- [ ] Mixpanel инициализируется
- [ ] Cloudflare работает
- [ ] OAuth работает (Google, Telegram, Facebook)
- [ ] Email отправка работает
- [ ] Нет ошибок в консоли
- [ ] Секреты не в client bundle

**Команда:**

- [ ] Migration guide готов
- [ ] Команда проинформирована
- [ ] Ответы на вопросы даны

---

## Troubleshooting

### Проблема: После удаления runtimeConfig приложение не запускается

**Решение:**

1. Проверить, что все файлы мигрированы (Итерации 2, 3)
2. Проверить логи ошибок
3. Если нужно - откатить `nuxt.config.ts`:
   ```bash
   git checkout HEAD -- nuxt.config.ts
   ```

### Проблема: TypeScript ошибки после миграции

**Решение:**

1. Проверить, что `types/env.d.ts` создан и актуален
2. Перезапустить TypeScript сервер в IDE
3. Выполнить:
   ```bash
   rm -rf .nuxt
   npm run dev
   ```

### Проблема: Секреты попали в client bundle

**Решение:**

1. Проверить, что приватные переменные БЕЗ префикса `NUXT_PUBLIC_`
2. Пересобрать production build:
   ```bash
   rm -rf .output
   npm run build
   ```

---

## Финальный чеклист всего PRD

Перед завершением PRD проверить:

### Код

- [ ] ✅ 0 использований `useRuntimeConfig()` в кодовой базе
- [ ] ✅ 0 секций `runtimeConfig` в `nuxt.config.ts`
- [ ] ✅ Все 5 файлов мигрированы
- [ ] ✅ Типы env созданы (`types/env.d.ts`)
- [ ] ✅ Валидация env создана (`server/utils/validate-env.ts`)
- [ ] ✅ Валидация интегрирована (`server/plugins/init.ts`)

### Тестирование

- [ ] ✅ Dev сервер запускается
- [ ] ✅ Production build успешен
- [ ] ✅ База данных работает
- [ ] ✅ API endpoints работают
- [ ] ✅ Mixpanel работает
- [ ] ✅ Cloudflare работает
- [ ] ✅ OAuth работает
- [ ] ✅ TypeScript компилируется
- [ ] ✅ Секреты не в bundle

### Документация

- [ ] ✅ `.env.example` актуален
- [ ] ✅ `docs/ENV_MIGRATION.md` создан
- [ ] ✅ Документация обновлена
- [ ] ✅ README обновлен (если нужно)

### Команда

- [ ] ✅ Code review завершен
- [ ] ✅ Команда проинформирована
- [ ] ✅ Migration guide разослан

---

## Следующие шаги

После завершения этой итерации:

1. Проверить все Acceptance Criteria
2. Выполнить финальный чеклист
3. Создать commit: `feat: complete env migration - remove runtimeConfig`
4. Создать PR с описанием всех изменений
5. После апрува - замержить в master
6. Разослать команде `docs/ENV_MIGRATION.md`
7. Закрыть PRD как ✅ Completed

**🎉 Миграция завершена!**

---

[← Назад к итерациям](README.md) | [← Предыдущая: Итерация 3](iteration-03-client-migration.md) | [← Назад к оглавлению](../index.md)
