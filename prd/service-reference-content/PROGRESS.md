# Прогресс

## Батч 0 — пилот (10 карточек, ручной черновик)

Готово, текст на русском, формат утверждён пользователем 2026-07-17.

Лабораторные анализы: `complete-urinalysis`, `glucose`, `cholesterol`, `creatinine`, `c-reactive-protein`
Услуги: `ecg`, `abdomen-ultrasound`, `thyroid-ultrasound`, `complete-dental-cleaning`, `spirometry`

## Батч 1 — ещё 100 карточек (готово, через субагентов, 2026-07-17)

Кандидаты отобраны по покрытию клиниками (`clinic_lab_tests`/`clinic_medical_services`) с редакторской фильтрацией по YMYL-рискам (см. `index.md`). 8 субагентов запущены параллельно, каждый — своя тематическая группа.

| Группа | Кол-во | Статус |
|---|---|---|
| Лаб. анализы: биохимия/электролиты (A) | 12 | готово |
| Лаб. анализы: гематология/коагуляция/маркеры воспаления и сердца (B) | 12 | готово |
| Лаб. анализы: гормоны/витамины (C) | 11 | готово |
| Лаб. анализы: инфекции/аутоиммунные/PSA/беременность (D) | 10 | готово |
| Услуги: УЗИ/допплер/кардиология-мониторинг (E) | 15 | готово |
| Услуги: рентген + осмотры специалистов (F) | 18 | готово |
| Услуги: стоматология (G) | 10 | готово |
| Услуги: физиотерапия/гинекология/гастро (H) | 12 | готово |

Итого новых: 45 анализов + 55 услуг = 100. Вместе с пилотным батчем 0 — **50 анализов + 60 услуг = 110 карточек**.

Все slug сверены с `lab_tests`/`medical_services` в БД (совпадение 50/50 и 60/60), JSON провалидирован (нет дублей, все 5 полей заполнены на `ru`).

Результат — `data/entity-reference/lab-tests.json` и `data/entity-reference/medical-services.json` (только `ru`, остальные локали позже).

## Батч 2 — перевод на 5 языков (готово, 2026-07-17)

Все 110 карточек переведены на en/sr/sr_cyrl/de/tr (плюс исходный ru) — итого 6 локалей × 5 полей × 110 карточек = 3300 строк, без пропусков.

- en/sr/de/tr — переведены через 8 параллельных субагентов (те же тематические группы A-H) + вручную для пилотного батча 0 (10 карточек).
- sr_cyrl — не переведён отдельно, а механически сгенерирован из sr транслитерацией через существующую `common/serbian-transliteration.ts` (digraph-карта латиница↔кириллица) — гарантирует 100% смысловое совпадение sr/sr_cyrl без риска рассинхрона при будущих правках (см. конвенцию из i18n-audit).

## Батч 3 — схема БД выбрана (2026-07-17)

Из 3 предложенных вариантов (единая таблица entity_type+entity_id+language / широкие колонки прямо в lab_tests-medical_services / две отдельные таблицы с настоящим FK) выбран **вариант 3**: две таблицы `lab_test_reference_info` + `medical_service_reference_info`, каждая с честным FK на родителя и 30 текстовыми колонками (5 полей × 6 локалей, суффиксы как у `name_en/name_sr/...`).

Миграция схемы создана: `server/sql/migrations/009-entity-reference-info.sql`. **Не применена** — по конвенции проекта пользователь применяет SQL-миграции сам.

## Батч 4 — место вывода на странице выбрано (2026-07-18)

Разобрались, что «табы» в `EntityPage` — это якорная навигация со скроллом, а не переключение видимости: все секции всегда в DOM, вопрос «таб vs блок для индексации» неактуален. Выбран вариант: справка — первая `EntityPageSection` в списке (перед «Клиниками»), со своей записью в `tabs`. Подробности и затронутые файлы — в `index.md` → «Вывод на странице».

## Батч 5 — миграция применена, скрипт импорта готов (2026-07-18)

Миграция 009 применена пользователем к БД (лог — `server/sql/migrations/APPLIED.md`).

Скрипт `scripts/entity-reference/build-entity-reference-sql.mjs` собирает
`server/sql/migrations/insert-entity-reference-info.sql` из
`data/entity-reference/{lab-tests,medical-services}.json` — FK по slug через
подзапрос, `INSERT ... ON DUPLICATE KEY UPDATE` (безопасно перезапускать после
правки текстов/переводов). Сам импорт в БД ещё **не выполнен** — файл создан,
применяет пользователь той же командой, что в шапке (`mysql ... < insert-entity-reference-info.sql`).

## Батч 6 — вывод на странице реализован (2026-07-18)

Пользователь применил `insert-entity-reference-info.sql` (50 + 60 строк в БД).

Реализация:
- `server/common/utils.ts` → `buildReferenceInfo(row, locale)` — выбор локализованного поля с фолбэком sr→en (аналог `getLocalizedNameField`).
- `server/api/services/details.ts` и `server/api/labtests/details.ts` — доп. запрос к `medical_service_reference_info`/`lab_test_reference_info` по id, поле `referenceInfo` в ответе (`null`, если карточки нет).
- `interfaces/reference-info.ts` (`ReferenceInfo`) + добавлено в `ClinicServiceWithPrices.referenceInfo`.
- `components/entity-page/reference-section.vue` — новая секция (5 полей `dt/dd` + дисклеймер), свой i18n-блок на 6 языков, иконка `IconInfo`.
- Обе страницы (`pages/services/[serviceSlug]/index.vue`, `pages/labtests/[labTestSlug]/index.vue`): секция первая в `#sections`, таб `TabReference` первый в списке табов (когда `referenceInfo` есть).
- i18n: ключ `TabReference` добавлен в `i18n/medical-service.ts` и `i18n/labtest.ts` на всех 6 локалях.

Проверено вживую (dev-сервер, curl): секция рендерится первой перед «Клиниками», текст корректен на всех 6 локалях (en/ru/de/tr/sr/sr-cyrl), при отсутствии карточки (`24-hour-or-overnight-eeg-recording`) секция и таб не рендерятся — ошибок нет. Typecheck зелёный.

## Батч 7 — 5 карточек по запросу статей (2026-07-18)

Добавлены по конкретным ссылкам из статей о родах/беременности:
`services/amniocentesis`, `services/vaginal-delivery-anterior-occiput-presentation`,
`services/low-isthmic-cesarean-section-with-delivery`, `labtests/papp-a`,
`labtests/free-beta-hcg`. (`labtests/beta-hcg` уже существовал с батча 1.)

**YMYL-развилка:** PAPP-A и free-beta-hCG — маркеры пренатального скрининга с
расчётом риска, которые PRD изначально исключил из охвата (см. «YMYL-правила»
в index.md). Т.к. статьи ссылаются именно на них, пользователь выбрал
компромисс: писать карточки, но без интерпретации риска — поле `abnormal`
только «оценивается врачом вместе с остальными показателями скрининга»,
без упоминания трисомий/расчёта риска.

Итого теперь 52 анализа + 63 услуги = 115 карточек, все 6 локалей, все поля
заполнены (проверено скриптом). `sr_cyrl` для новых карточек сгенерирован
транслитерацией — токен `PAPP-A` транслитерируется нормально («ПАПП-А», без
коллизий), `HCG` защищён (как в существующей карточке `beta-hcg`, где `HCG`
тоже записан заглавными и не транслитерируется).

SQL-импорт пересобран (`node scripts/entity-reference/build-entity-reference-sql.mjs`) —
`insert-entity-reference-info.sql` теперь на 115 карточек, `ON DUPLICATE KEY
UPDATE` — можно применять поверх уже загруженных 110 без дублей.

## Проверка на проде (2026-07-26)

Обновлённый `insert-entity-reference-info.sql` **применён** — все 115 карточек в проде.
Проверены поштучно все 5 карточек батча 7 плюс контрольные из батчей 0–1:

| URL | Секция | Дисклеймер |
| --- | --- | --- |
| `/services/amniocentesis?lang=ru` | ✅ | ✅ |
| `/services/vaginal-delivery-anterior-occiput-presentation?lang=ru` | ✅ | ✅ |
| `/services/low-isthmic-cesarean-section-with-delivery?lang=ru` | ✅ | ✅ |
| `/labtests/papp-a?lang=ru` | ✅ | ✅ |
| `/labtests/free-beta-hcg?lang=ru` | ✅ | ✅ |
| `/services/ecg?lang=ru`, `/labtests/glucose?lang=ru` (контроль) | ✅ | ✅ |

Локаль на проде задаётся `?lang=ru`, а не префиксом пути: `/ru/services/...` отдаёт 200
с пустой оболочкой без контента — на этом легко ошибиться при повторной проверке.

## Батч 8 — тир A воркшита: 50 услуг (2026-07-26)

Источник кандидатов — `data/entity-reference/TODO-worklist.md`, тир A (услуги без
карточки с максимальным покрытием клиниками). Все 50 слагов сверены с
`medical_services` (50/50), ни у одной не было записи в
`medical_service_reference_info`.

Написаны сразу на 5 языках (ru/en/sr/de/tr), 7 тематических групп:
инъекции и инфузии (7), УЗИ/допплер/гинекология (7), ЛОР/аллергология/пульмонология (5),
стоматология (9), осмотры специалистов (11), физиотерапия и ортопедия (5),
рентген/эндоскопия/хирургия (6).

**Формат для лечебных процедур.** У операций, инъекций и протезирования нет
«отклонения от нормы» — поле `abnormal` там, как и в батче 7, отвечает на вопрос
«что оценивает врач и когда обращаться раньше срока» (осложнения, красные флаги,
кто принимает решение). Диагностические позиции (аудиометрия, тимпанометрия,
допплер, рентген, УЗИ) сохраняют исходный смысл поля.
Отдельный случай — `blood-collection-fee`: это не процедура, а строка прайса,
и карточка честно это называет, а `abnormal` отсылает к самим анализам.

`sr_cyrl` сгенерирован транслитерацией из `sr`; в `PROTECTED_LATIN_TOKENS`
скрипта `scripts/entity-reference/generate-sr-cyrl.mjs` добавлены `HCV`, `TPHA`,
`PAPP-A` — они уже были защищены в данных лабораторных карточек, но в списке
скрипта отсутствовали (всплыло бы на `--force`). Проверено: в новых карточках
латиницей осталось только `PSA` (в двух урологических), `TSH` → `ТСХ` — как в
существующих `free-t3/free-t4`.

Батч лежит в **отдельном файле** `data/entity-reference/medical-services-batch-tierA-01.json`
по конвенции из обновлённого README (сборщик подхватывает `medical-services*.json`) —
чтобы параллельные сессии не затирали друг другу правки в общем массиве.

## Тир C воркшита — 29 услуг (2026-07-26)

Тир C — позиции, которые **уже получают показы в Google**, но карточки не имели:
`orthofed-method` (22 показа), `gynecological-specialist-examination-with-ultrasound` (17),
`4d-pregnancy-ultrasound` (14), `laparoscopic-hernia-operation` (13),
`ophthalmology-consultation` (12) и далее по убыванию. Взяты первыми именно
поэтому: спрос подтверждён, не гипотетический.

Все 29 слагов сверены с `medical_services` (29/29), ни у одной не было записи в
`medical_service_reference_info`, пересечений с `lab_tests` нет. Написаны сразу
на 5 языках (ru/en/sr/de/tr), `sr_cyrl` сгенерирован транслитерацией.
Файл — `data/entity-reference/medical-services-batch-tierC-01.json`.

**`orthofed-method` — фактура вытянута из первоисточника.** Метод нигде толком
не описан, предлагают его только физиоцентры Endorfin. По их же странице
(`endorfin.me/terapija/ortofed-terapija/`) это аппаратная методика коррекции
деформаций позвоночника: трёхмерный подход через фиксацию, элонгацию и
деротацию, фиксаторы стабилизируют таз и позвоночник, толкатель задаёт деротацию
и контрфлексию под управлением компьютерной программы. Длительность и число
сеансов источник не указывает — в карточке честно написано, что их определяет
физиотерапевт, а не выдумано «20–30 минут».

**Формат для лечебных и «непроцедурных» позиций** — как в батчах 7–8: у массажа,
LASIK, склеротерапии, ринопластики, операций поле `abnormal` отвечает
«что ожидаемо, когда обращаться раньше срока, кто принимает решение».
Отдельные случаи:
- `iv-therapy-service` — это строка прайса за работу медсестры, а не препарат;
  карточка так и называет, препарат оплачивается отдельно.
- `hetero-anamnesis` — часть осмотра, а не исследование с результатом.
- `hearing-loss-percentage-calculation-fowler-sabine-formula` — расчёт по уже
  готовой аудиограмме, а не обследование; `abnormal` разводит «степень» и «причину».
- `ivf-laboratory-procedure` — этап без участия пациента, подготовка относится
  к предыдущим этапам цикла.

**`PAPP-A` возвращён к транслитерации.** В `PROTECTED_LATIN_TOKENS` он был
добавлен в батче 8 с обоснованием «уже защищён в данных» — это не так:
в проде стоит «ПАПП-А» кириллицей (решение батча 7, проверено на проде 2026-07-26).
`--force` молча переписывал 3 поля в `papp-a` и `free-beta-hcg`. Токен убран из
списка с комментарием, данные откачены к прод-версии.

## Тир B воркшита — 82 услуги (2026-07-26)

Тир B — плановые потребительские позиции, где человек сам выбирает клинику и
сравнивает цены: протезирование, имплантация, ортодонтия, эстетика, косметология,
пластическая хирургия. Написаны сразу на 5 языках (ru/en/sr/de/tr), 8 тематических
батчей `medical-services-batch-tierB-01…08.json`:

| Батч | Тема | Кол-во |
|---|---|---:|
| tierB-01 | Съёмное протезирование и перебазировки | 11 |
| tierB-02 | Коронки, мосты, коронки на имплантатах | 13 |
| tierB-03 | Имплантация, планирование, All on 4/6 | 12 |
| tierB-04 | Ортодонтия: брекеты, аппараты, ретейнеры | 12 |
| tierB-05 | Эстетическая стоматология: отбеливание, виниры, DSD | 5 |
| tierB-06 | Косметология: филлеры, ботулотоксин, PRP, липолиз | 10 |
| tierB-07 | Пластическая хирургия | 8 |
| tierB-08 | Проктология, урология, офтальмология, прочее | 11 |

Все 82 слага сверены с `medical_services` (82/82, read-only SELECT по локальной
БД), ни у одной не было записи в `medical_service_reference_info`. Валидация:
276 карточек во всех файлах, дублей по слагу нет, 6 локалей × 5 полей заполнены
без пропусков, в собранном SQL 0 `NULL`.

**Поле `abnormal` для плановых вмешательств** — по формату батчей 7-8: «что
ожидаемо в первые дни и когда обращаться раньше срока». Для филлеров и
ботулотоксина туда вынесены настоящие красные флаги (побеление кожи, резкая боль,
нарушение зрения — при филлерах; опущение века и двоение — при ботулотоксине),
без обещаний результата. Отдельные случаи:
- `implant-suprastructure`, `dental-bridge-abutment`, `hyaluronic-fillers-1ml`,
  `botox-single-region` — это единицы прайса, а не процедуры; карточки так и
  называют и советуют заранее уточнить, что входит в цену.
- `fixation-transducer-for-icp-monitoring-in-various-etiologies` попал в тир B
  ошибочно (это реанимационная манипуляция, а не плановая покупка): карточка
  честно пишет, что подготовку делает персонал стационара, а цифры давления вне
  клинического контекста не интерпретируются.
- `professor-cardiologist-examination` — разница только в звании врача и
  длительности приёма; `abnormal` отсылает при боли в груди к экстренной помощи,
  а не к плановой записи.

**Дубли в справочнике услуг** (не ошибки контента, но кандидаты на слияние в БД):
`skeletal-partial-denture` / `skeletal-denture`, `complete-denture` /
`complete-acrylic-denture`, `hyaluronic-acid-filler` / `hyaluronic-filler` /
`hyaluronic-fillers-1ml`, `botox-injection` / `botox-single-region` /
`vistabel-botox`, `orthodontic-retainer` / `retainer-placement-per-arch` /
`retention-aligner-fabrication`. Карточки написаны с разными акцентами, чтобы
страницы не выглядели копиями.

**`PROTECTED_LATIN_TOKENS` расширен** брендами и обозначениями тира B:
`Nobel Biocare`, `Straumann`, `Bredent`, `Invisalign`, `Vistabel`, `Botox`,
`All on 4`, `All on 6`, `E-max`, `CAD/CAM`, `CoCr`, `PRP`, `IOL` плюс римские
цифры стадий `I-III`/`I-II`/`II-III`. Без этого транслитерация давала «Ботоx»,
«Нобел Биоцаре», «Алл он 4», «Е-маx», «ЦАД/ЦАМ», «И-ИИИ». `--force` после правки
списка сверен: карточки тиров A и C и прод-файлы `lab-tests.json` /
`medical-services.json` побайтово не изменились (в их текстах этих токенов нет),
`PAPP-A` остался транслитерированным, как в проде.

## Дальше

- ✅ ~~Применить пересобранный `insert-entity-reference-info.sql`~~ — **применено, все 276 карточек в проде.** Сверено 2026-07-30 в рамках ревью SEO-аудита: 10 слагов из поздних тиров (`amniocentesis`, `stress-echocardiography`, `metal-free-zirconia-ceramic-crown`, `acupuncture`, `iud-insertion`, `gastroscopy-without-anesthesia`, `dental-bridge-abutment`, `in-office-teeth-whitening-single-arch`, `filling-retention-with-fiber-post`, `cardiologist-examination-with-ecg`) отдают справку на проде, текст побайтово совпадает с `data/entity-reference/medical-services.json`.
- Остаток воркшита `data/entity-reference/TODO-worklist.md`: тир D (52 — сначала нужен отдельный формат для `medical-certificate-*`, справка из 5 полей к документам не подходит).
- Проверка всех **276** карточек на фактическую точность и тон вручную (особенно чувствительные: ВИЧ/гепатиты/сифилис, PSA, бета-ХГЧ, PAPP-A, кольпоскопия/PAP-тест, острые кардио/тромбо-маркеры, акушерские) — **единственный оставшийся пункт PRD**. Требует носителя языка / медика, агентом не закрывается.

## Батчи 8–10: тиры A/B/C (2026-07-26)

Три параллельные сессии, **+161 карточка услуг** (было 63 → стало 224; анализы без изменений, 52).
Приоритеты взяты из `data/entity-reference/TODO-worklist.md`, собранного из БД + выгрузок GSC:

| Тир | Что | Сделано |
| --- | --- | --- |
| A | добивка топ-100 услуг по покрытию клиниками | 50 / 50 |
| B | плановые потребительские (стоматология, эстетика, зрение, фертильность) — брекеты, импланты, протезы, отбеливание, ринопластика | 82 / 82 |
| C | уже получают показы в Google, но карточки не было | 29 / 29 |
| D | `medical-certificate-*` (52 шт.) | не брали — нужен другой формат |

### Результаты проверки

Проверено скриптом по всем 276 карточкам (224 услуги + 52 анализа):

- дубли slug — нет; все slug резолвятся в БД (FK не упадёт);
- полнота — 0 карточек с пропусками, везде 6 локалей × 5 полей, пустых строк нет;
- уникальность — среди новых карточек **0 повторов** в `what`/`how`/`when`; 3 повтора в `prep`/`abnormal` и все обоснованные (массаж частичный/полный, LASIK один глаз/оба — подготовка и правда одинаковая);
- 0 совпадений текста с уже стоящими в проде 115 карточками (копипасты нет);
- новые тексты длиннее старых: `what` 102 против 82 символов, `how` 173 против 94, `prep` 140 против 80;
- YMYL — 19 срабатываний регулярки оказались ложными: ловилось «Диагноз ставит врач», «не указывает на конкретное заболевание», «список лекарств с дозировками» (что взять с собой). Числовых референсов и диагностических утверждений нет;
- транслитерация — 0 артефактов, 0 слов со смешением кириллицы и латиницы. `PROTECTED_LATIN_TOKENS` в `scripts/entity-reference/generate-sr-cyrl.mjs` дополнен брендами тира B (Nobel Biocare, Straumann, Bredent, Invisalign, Botox, Vistabel), материалами (E-max, CoCr, CAD/CAM) и **римскими цифрами** (`I-III` без защиты превращалось в «И-ИИИ»).

Ложные срабатывания YMYL-регулярки — ожидаемое поведение: она ищет подстроку «диагноз», а правильная формулировка «диагноз ставит врач» её содержит. При следующих прогонах смотреть на контекст, а не на счётчик.

### Слияние

Батч-файлы влиты в `data/entity-reference/medical-services.json` в том же порядке, в каком их читал сборщик (батчи по алфавиту, затем базовый файл), поэтому сгенерированный SQL **побайтово совпал** с тем, что собирался до слияния — проверено `diff`. Батч-файлы удалены.

`insert-entity-reference-info.sql` пересобран: **276 INSERT** (52 анализа + 224 услуги), `ON DUPLICATE KEY UPDATE`, кавычки сбалансированы. Применять поверх стоящих в проде 115 безопасно.

```
mysql -u docta_admin -p --default-character-set=utf8mb4 docta_me < server/sql/migrations/insert-entity-reference-info.sql
```
